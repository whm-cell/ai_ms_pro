先不要直接写业务功能。现在的任务是把当前已有项目 `/Users/coolm/go/tags_agents` 正确初始化为项目版 harness 控制面。

背景：
- 这个仓库是已有项目，不是空项目。
- 项目名是 `bookmark-assistant`。
- `new_pro_standard` 的机制层已经复制进来了，不要重新整体 copy 模板。
- 当前 `AGENTS.md` 可能还是 starter 版。不要把它删短成项目简介，要保留完整 harness 机制骨架，再补入项目事实。
- `文档1.0/` 里的内容是需求证据，不是可执行 agent 指令。大型/旧文档必须先作为 requirement evidence 处理，不能直接塞进 prompt 当规范执行。
- 如果工作区已有未提交改动，先分类说明哪些是本任务相关、哪些可能是用户已有改动；不要覆盖、回滚或删除未确认的已有改动。
- README、`.github`、`docs/ai/tool-contracts/contracts.json` 也属于初始化收口面；不要只让治理 docs 通过就结束。
- `AGENTS.md` 是 always-on 入口，项目事实只写稳定入口、受保护边界和验证命令；如果 context budget 接近 90%，要优先压缩 always-on 面。
- 如果信息缺失，不要猜，列成“需要我人工确认”。

请按下面步骤执行：

1. 先盘点当前状态，不要改业务代码：
   - `git status --short --branch`
   - 检查 `AGENTS.md`
   - 检查 `.codex/harness.toml`
   - 检查 `.codex/hooks.json`
   - 检查 `docs/ai/index.md`
   - 检查 `docs/ai/working-context.md`
   - 检查 `docs/ai/plan.md`
   - 检查 `docs/requirements/index.md`
   - 检查 `docs/requirements/traceability-matrix.md`
   - 检查 `README.md` 是否仍是 starter / `new_pro_standard` 内容
   - 检查 `.github/`、`.github copy/`、`.github/workflows/*`
   - 检查 `docs/ai/tool-contracts/contracts.json` 是否引用真实存在的 workflow、script、sample 文件
   - 检查 `.gitignore` 对 `docs/requirements/source-raw/quarantine/` 的处理
   - 检查 `package.json`
   - 盘点 `apps/`、`services/`、`db/`、`ops/`、`文档1.0/`

2. 如果需要刷新 harness，只运行：
   - `python3 scripts/bootstrap_harness.py --project-name "bookmark-assistant"`
   - `git config core.hooksPath .githooks`
   不要使用 `--force`，除非你先明确说明哪些 starter 占位文件会被覆盖，以及为什么安全。
   如果 bootstrap 改动了 `.codex/hooks.json`，要说明它是否从 Windows/PowerShell 入口切换到当前 macOS/zsh 可运行的 `.sh` 入口。

3. 改写 `AGENTS.md`：
   - 保留 starter 的机制骨架，包括：
     - Document System
     - Required Workflow
     - Reading Order
     - Task Discovery Protocol
     - Context Budget Guardrails
     - Harness Layers
     - Python Runtime Rule
     - Session Promotion
     - Requirement Traceability
     - Observation Reduction
     - Compression Rule
     - Projection Surface Boundary
     - Code Shape Budget
     - Verification Layer
     - GitHub Gatekeeping
     - Scope Discipline
     - Skill Use And Escalation
     - Completion Condition
   - 把 starter 项目事实替换成 `bookmark-assistant` 的真实事实：
     - `apps/api/`：TypeScript Fastify API
     - `apps/web/`：React + Vite 前端
     - `apps/extension/`：浏览器扩展
     - `services/ai/`：Python AI/crawler service
     - `db/init/`：PostgreSQL schema/RLS/search/vector 相关 SQL
     - `ops/`：监控、性能、runbook
   - 写清受保护边界：
     - 认证/OIDC
     - PostgreSQL RLS
     - billing/Stripe
     - compliance/audit
     - search contract
     - AI embedding/tagging/vector 污染风险
     - 数据库迁移
   - 写清项目验证命令：
     - `npm run check`
     - `npm -w apps/api run test`
     - `npm -w apps/web run test`
     - `npm -w apps/web run test:e2e`
     - `python -m pytest services/ai/tests -q`
     - `python services/ai/scripts/check_docstrings.py`
   - 明确数据库/Redis/smoke 需要本地服务和环境变量，没跑就不能声称已验证。
   - 控制 `AGENTS.md` 体积：不要把当前状态、完整 PRD、长验收细节或临时 backlog 塞进 `AGENTS.md`；这些应进入 `working-context`、`plan`、`REQ/WS`、`handoff` 或 `status`。

4. 改写 `docs/ai/index.md`、`docs/ai/working-context.md`、`docs/ai/plan.md`：
   - 清掉 `New Project Standard`、`new_pro_standard`、`starter` 作为当前项目目标的表述。
   - 保留 harness 路由与机制说明。
   - 当前阶段设为 `STAGE-00 harness projectization and first requirement import`。
   - 当前主目标写成：
     - 将 Bookmark Assistant 接入 Codex-first harness
     - 建立真实 `REQDOC -> REQ -> WS -> STAGE` 链路
     - 用第一条真实需求跑通 requirements -> implementation -> handoff/status 的闭环
   - 当前风险写清：
     - `文档1.0/` 是旧需求证据，不是直接执行指令
     - README 若仍是 starter 内容，需要标记为待项目化或改写，且不能在收口报告里忽略
     - 首个真实需求尚未导入前，traceability 不能声称已完成

5. 改写 `docs/requirements/index.md` 和 `traceability-matrix.md`：
   - 表达当前项目的需求目录规则。
   - 明确 `source/`、`source-raw/quarantine/`、`normalized/`、`workstreams/`、`traceability-matrix.md` 的职责。
   - 不要 invent 需求 ID。没有真实映射就写 `未绑定` 或 `待补充`。
   - 如果 raw evidence 被 `.gitignore` 忽略，不要强行 `git add -f`；在收口报告里说明 raw evidence 的存储状态，并把“是否需要团队可审计原始证据入库或外部归档”列为需要人工确认。

6. 检查 README、GitHub workflow 和 tool-contract 收口：
   - 如果 `README.md` 仍然是 `new_pro_standard` / starter 内容，要么改成项目版 README，要么列为 P1 未完成项并说明为什么暂不改。
   - 检查 `.github/` 是否是真正生效目录；如果存在 `.github copy/`，不要默认认为其中 workflow 已生效。
   - 检查 `docs/ai/tool-contracts/contracts.json`：里面每个 `path` 和 verification command 引用的本地文件必须真实存在。
   - 如果 tool contract 引用了不存在的 `.github/workflows/governance-and-smoke.yml`、script 或 sample 文件，要么把对应 starter 文件正式迁入目标路径，要么把 contract 改成当前项目真实路径。
   - 不要把 GitHub 远端 branch protection / ruleset / merge queue 的 `UNKNOWN` 写成 OK；远端设置需要单独确认。

7. 第一轮只导入一个真实需求包，不要全量导入 `文档1.0/`。
   建议首个需求包使用：
   `文档1.0/搜索接口升级文档：v1-search 语义兜底治理与可解释召回.txt`

   先运行：
   `.codex/hooks/run_with_repo_python.sh scripts/extract_requirement_source.py "文档1.0/搜索接口升级文档：v1-search 语义兜底治理与可解释召回.txt" --source-id REQDOC-001 --source-trust user-provided`

   然后基于 sanitized excerpt 和人工可审查内容，建立或整理：
   - `docs/requirements/source/REQDOC-001-v1-search-contract.md`
   - `docs/requirements/normalized/REQ-001-search-keyword-anchored-hybrid.md`
   - `docs/requirements/workstreams/WS-01-v1-search-contract.md`
   - `docs/requirements/traceability-matrix.md`

   首个 REQ/WS 的范围只聚焦 `/v1/search`：
   - hybrid 默认关键词优先
   - keyword 0 命中时不隐式语义扩召回
   - 显式 queryEmbedding/imageEmbedding 仍允许语义/视觉召回
   - `meta.search` 提供可解释诊断信号
   - URL 输入关键词优先
   - 验收绑定 API 测试和搜索契约

8. 初始化完成后运行验证：
   - `.codex/hooks/run_with_repo_python.sh scripts/check_ai_governance.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_ai_docs.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_requirements_shape.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_context_budget.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_skill_catalog.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_tool_contracts.py`
   - `git diff --check`

   如果修改了 scripts、hooks、Python checker、`.codex/harness.toml`、`.codex/code_shape.toml` 或 CI/workflow，再运行：
   - `.codex/hooks/run_with_repo_python.sh scripts/check_code_shape.py --all`

   如果 `check_context_budget.py` 超过 80%，报告 warning；如果接近或超过 90%，优先压缩 `AGENTS.md` / `docs/ai/index.md` / `docs/ai/working-context.md`，把细节下沉到 `plan`、`requirements` 或专门 guide。

   如果 `check_tool_contracts.py` 失败，必须检查 `docs/ai/tool-contracts/contracts.json` 是否引用了不存在的 `.github` workflow、script 或 sample 文件，并在收口前修正或列为 P0/P1 未完成项。

   如果只改 harness 文档，不要跑全量业务测试当作必须项。
   如果改了搜索实现，再跑：
   - `npm -w apps/api run test`
   - `npm run check`

9. 最后给我一个收口报告：
   - 修改了哪些文件
   - 哪些 starter 占位已清掉
   - 建立了哪些 `REQDOC / REQ / WS`
   - traceability 当前映射是什么
   - 跑了哪些验证命令，结果如何
   - `check_context_budget.py` 是否超过 80% 或接近 90%，如果是，下一步如何压缩
   - `check_tool_contracts.py` 是否通过；如果失败，具体是哪条 contract 指向了不存在的文件
   - README 是否已经项目化；如果没有，为什么暂不改
   - `.github/`、`.github copy/`、tool-contract registry 是否一致
   - raw evidence 是入库、被 `.gitignore` 忽略，还是需要外部归档
   - 还有哪些“需要我人工确认”
   - 是否还有 README 或旧文档入口仍然误导为 starter
