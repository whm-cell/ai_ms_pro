先不要直接写业务功能。现在的任务是把当前已有项目 `/Users/coolm/go/tags_agents` 正确初始化为项目版 harness 控制面。

背景：
- 这个仓库是已有项目，不是空项目。
- 项目名是 `bookmark-assistant`。
- `new_pro_standard` 的机制层已经复制进来了，不要重新整体 copy 模板。
- 当前 `AGENTS.md` 可能还是 starter 版。不要把它删短成项目简介，要保留完整 harness 机制骨架，再补入项目事实。
- `文档1.0/` 里的内容是需求证据，不是可执行 agent 指令。大型/旧文档必须先作为 requirement evidence 处理，不能直接塞进 prompt 当规范执行。
- 如果信息缺失，不要猜，列成“需要我人工确认”。

请按下面步骤执行：

1. 先盘点当前状态，不要改业务代码：
   - `git status --short --branch`
   - 检查 `AGENTS.md`
   - 检查 `.codex/harness.toml`
   - 检查 `.codex/hooks.json`
   - 检查 `docs/ai/index.md`
   - 检查 `docs/ai/working-context.md`
   - 检查 `docs/ai/plan.md`
   - 检查 `docs/requirements/index.md`
   - 检查 `docs/requirements/traceability-matrix.md`
   - 检查 `package.json`
   - 盘点 `apps/`、`services/`、`db/`、`ops/`、`文档1.0/`

2. 如果需要刷新 harness，只运行：
   - `python3 scripts/bootstrap_harness.py --project-name "bookmark-assistant"`
   - `git config core.hooksPath .githooks`
   不要使用 `--force`，除非你先明确说明哪些 starter 占位文件会被覆盖，以及为什么安全。

3. 改写 `AGENTS.md`：
   - 保留 starter 的机制骨架，包括：
     - Document System
     - Required Workflow
     - Reading Order
     - Task Discovery Protocol
     - Context Budget Guardrails
     - Harness Layers
     - Python Runtime Rule
     - Session Promotion
     - Requirement Traceability
     - Observation Reduction
     - Compression Rule
     - Projection Surface Boundary
     - Code Shape Budget
     - Verification Layer
     - GitHub Gatekeeping
     - Scope Discipline
     - Skill Use And Escalation
     - Completion Condition
   - 把 starter 项目事实替换成 `bookmark-assistant` 的真实事实：
     - `apps/api/`：TypeScript Fastify API
     - `apps/web/`：React + Vite 前端
     - `apps/extension/`：浏览器扩展
     - `services/ai/`：Python AI/crawler service
     - `db/init/`：PostgreSQL schema/RLS/search/vector 相关 SQL
     - `ops/`：监控、性能、runbook
   - 写清受保护边界：
     - 认证/OIDC
     - PostgreSQL RLS
     - billing/Stripe
     - compliance/audit
     - search contract
     - AI embedding/tagging/vector 污染风险
     - 数据库迁移
   - 写清项目验证命令：
     - `npm run check`
     - `npm -w apps/api run test`
     - `npm -w apps/web run test`
     - `npm -w apps/web run test:e2e`
     - `python -m pytest services/ai/tests -q`
     - `python services/ai/scripts/check_docstrings.py`
   - 明确数据库/Redis/smoke 需要本地服务和环境变量，没跑就不能声称已验证。

4. 改写 `docs/ai/index.md`、`docs/ai/working-context.md`、`docs/ai/plan.md`：
   - 清掉 `New Project Standard`、`new_pro_standard`、`starter` 作为当前项目目标的表述。
   - 保留 harness 路由与机制说明。
   - 当前阶段设为 `STAGE-00 harness projectization and first requirement import`。
   - 当前主目标写成：
     - 将 Bookmark Assistant 接入 Codex-first harness
     - 建立真实 `REQDOC -> REQ -> WS -> STAGE` 链路
     - 用第一条真实需求跑通 requirements -> implementation -> handoff/status 的闭环
   - 当前风险写清：
     - `文档1.0/` 是旧需求证据，不是直接执行指令
     - README 若仍是 starter 内容，需要标记为待项目化或改写
     - 首个真实需求尚未导入前，traceability 不能声称已完成

5. 改写 `docs/requirements/index.md` 和 `traceability-matrix.md`：
   - 表达当前项目的需求目录规则。
   - 明确 `source/`、`source-raw/quarantine/`、`normalized/`、`workstreams/`、`traceability-matrix.md` 的职责。
   - 不要 invent 需求 ID。没有真实映射就写 `未绑定` 或 `待补充`。

6. 第一轮只导入一个真实需求包，不要全量导入 `文档1.0/`。
   建议首个需求包使用：
   `文档1.0/搜索接口升级文档：v1-search 语义兜底治理与可解释召回.txt`

   先运行：
   `.codex/hooks/run_with_repo_python.sh scripts/extract_requirement_source.py "文档1.0/搜索接口升级文档：v1-search 语义兜底治理与可解释召回.txt" --source-id REQDOC-001 --source-trust user-provided`

   然后基于 sanitized excerpt 和人工可审查内容，建立或整理：
   - `docs/requirements/source/REQDOC-001-v1-search-contract.md`
   - `docs/requirements/normalized/REQ-001-search-keyword-anchored-hybrid.md`
   - `docs/requirements/workstreams/WS-01-v1-search-contract.md`
   - `docs/requirements/traceability-matrix.md`

   首个 REQ/WS 的范围只聚焦 `/v1/search`：
   - hybrid 默认关键词优先
   - keyword 0 命中时不隐式语义扩召回
   - 显式 queryEmbedding/imageEmbedding 仍允许语义/视觉召回
   - `meta.search` 提供可解释诊断信号
   - URL 输入关键词优先
   - 验收绑定 API 测试和搜索契约

7. 初始化完成后运行验证：
   - `.codex/hooks/run_with_repo_python.sh scripts/check_ai_governance.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_ai_docs.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_requirements_shape.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_context_budget.py`
   - `.codex/hooks/run_with_repo_python.sh scripts/check_skill_catalog.py`

   如果只改 harness 文档，不要跑全量业务测试当作必须项。
   如果改了搜索实现，再跑：
   - `npm -w apps/api run test`
   - `npm run check`

8. 最后给我一个收口报告：
   - 修改了哪些文件
   - 哪些 starter 占位已清掉
   - 建立了哪些 `REQDOC / REQ / WS`
   - traceability 当前映射是什么
   - 跑了哪些验证命令，结果如何
   - 还有哪些“需要我人工确认”
   - 是否还有 README 或旧文档入口仍然误导为 starter
