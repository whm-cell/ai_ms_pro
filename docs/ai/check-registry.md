# Check Registry

更新时间：2026-06-16
状态：已确认

## 作用

本文件记录 harness checks 的治理等级，避免把所有提醒都升级为 blocking，也避免只靠口头约定判断哪些检查必须跑。

## 等级定义

- `advisory`：只提示风险或压缩机会，不阻断。
- `review-required`：需要 PR 作者或 reviewer 明确看过；是否阻断由当前阶段决定。
- `blocking-candidate`：具备升级为 blocking 的候选条件，但必须先满足真实样本、误报率和修复路径要求。
- `blocking`：CI、hook 或远端规则已经强制；失败必须修复或显式记录例外。

## 当前分级

| Check | Level | CI coverage | 升级条件 |
| --- | --- | --- | --- |
| `check_ai_governance.py` | `blocking` | governance job / Stop hook | 已强制，继续保持 |
| `check_code_shape.py` | `blocking-candidate` | governance job uses `--all`; hooks use `--staged`; scope includes Python / TS / JS / CSS / SCSS / SQL / Rust / shell / PowerShell | 新增大文件误报可控后保持或收紧；Next-style source trees 已进入 file-level budget；tests 与 fixture/cases/mock data 使用 path-specific 单文件预算 |
| Ruff Python linter / whitespace | `blocking` | governance job installs `.codex/requirements.txt`, runs `git diff --check`, then runs `python3 -m ruff check .codex/hooks scripts tests`; scope is `E9` plus Pyflakes `F` | 保持当前 blocking scope；`PLR2004`、`C901`、`PLR0912`、`PLR0915` 只能先作为 no-write / review-required 样本收集，误报率、修复路径和 CI 成本可控后再讨论升级 |
| Evidence-based coding standards | `review-required` | manual / `$repo-governed-coding` checklist | 覆盖魔法值、复杂度、长函数/大类职责、重复代码风险、命名和公共抽象触发条件；后续若引入 Ruff 或 JS/TS lint，必须先进入 blocking-candidate burn-in |
| Enterprise Code Boundary standards | `review-required` | manual / `$enterprise-code-boundary-maintenance` checklist | 覆盖 logging/redaction、error contract、runtime side effect 和 config contract routing；v1 只做规范索引和 changed-file follow-up，不新增 checker、不声明 SIEM/DLP、生产 observability、全局错误码平台、service mesh 或远端 side-effect 审计 |
| `check_pr_touch_conflicts.py` | `blocking-candidate` | PR job blocks confirmed high-risk overlap; GitHub API `UNKNOWN` stays visible but non-blocking during burn-in | 两次真实多人 PR 样本证明收益后收紧 |
| `check_github_guardrails.py` | `review-required` | manual / PR review evidence | private Free 下只证明本地/CI evidence 与 plan-limited `UNKNOWN`；升级 plan 或改 public 后再考虑 required-check 阻断 |
| `check_branch_hygiene.py` | `blocking` | PR summary runs `--strict --current-pr`; main push summary runs `--strict`; manual cleanup commands remain explicit; Actions token 无法读取 check rollup 时 failed-open-PR 审计降级为 NOTE | active PR 预算、failed open PR、stale branch 持续稳定后再考虑调整阈值 |
| WS-01 / WS-02 active browser smoke | `blocking` | smoke job runs `threejs_snake_smoke.py`, `threejs_snake_blackbox_smoke.py`, `harness_trace_console_smoke.py`, and `harness_trace_console_blackbox_smoke.py` | 已强制；WS-01 Three.js Snake 是当前 harness capability validation sample；WS-02 Harness Trace Console 是当前 governance UI sample；Playwright browser install 与 CLI package 版本继续固定 |
| `check_requirements_shape.py` | `blocking-candidate` | manual / follow-up summary | PRD 导入、raw evidence / source quarantine、`pending` source boundary 和 external-web / third-party / unknown 样本证明误报可控后升级；`source-evidence/raw-prd-evidence` 不作为 canonical REQDOC，缺 trust/instruction/sanitization metadata 为 review-required |
| `check_agent_trace_schema.py` | `blocking-candidate` | governance job; runtime stop-hook tests validate producer output against the schema; local export adapter runs against the sample | Stop producer 与本地 adapter 已接入；后续若扩大到真实 trace-file batch validation，再根据误报率决定是否升级为 blocking |
| `export_agent_trace.py` | `advisory` | governance job runs local sample export | 本地 `local-otel-json` adapter 只证明转换层；OpenAI / OTLP / MCP / A2A 真实互通需要单独 contract 和 ADR |
| `export_agent_trace.py --format otlp-http-json` | `advisory` | local/unit tests cover explicit endpoint send; default CI path remains no-network | OTLP HTTP JSON pilot 只证明显式 endpoint + `network_exported` 证据链；外部 collector / OpenAI / MCP / A2A 仍需单独 ADR |
| `check_remote_trace_interop_report.py` | `advisory` | governance job validates report shape; local/unit tests cover localhost vs verified-remote boundary | 用于区分 `local-only` / `pilot-remote` / `verified-remote` claim level；不自动证明广义 remote interop |
| `check_agent_eval_dataset.py` | `advisory` | governance job | 已包含 skill-harness 回归样本；真实 eval 执行、误报率、CI 成本和模型质量结论需要后续样本 |
| `run_agent_eval_dataset.py` | `advisory` | governance job runs `--dry-run`; selected local evals are manual | deterministic grader 已落地；真实 eval 执行、误报率、CI 成本和模型质量结论需要后续样本 |
| `check_task_outcome_eval_dataset.py` | `advisory` | governance job validates dataset shape | task outcome eval 只提供本地 benchmark 结构和 cost proxy，不等于 hosted grading 或模型最终质量结论 |
| `run_task_outcome_eval_dataset.py` | `advisory` | governance job runs `--dry-run`; executed outcome evals remain local | 输出 `task_outcome`、`overreach`、`resume_stability`、`guardrail_posture` 与成本 proxy；真实长期趋势仍需更多运行样本 |
| `check_agent_run_provenance.py` | `advisory` | manual / follow-up summary | 校验 local-first `agent-run-provenance/v1` 记录的 REQ/WS 绑定或显式 unbound、authority、canonical write、changed files、tool contracts、validation evidence 和 claim boundaries；不读取 raw runtime、不声明 GitHub plan 升级、Copilot cloud agent task、hosted trace、MCP/A2A、OpenAI sandbox 或外部 OTLP 互通 |
| `check_ci_agent_contract.py` | `advisory` | manual / follow-up summary | 校验 `ci-agent-contract/v1` 的 PR-only trigger、read-only/default-minimal permissions、禁用 pull_request_target / secrets / OIDC / repo write / PR comment / label / merge / release / deploy / external-send，并要求 hosted/cloud-agent 与 remote-enforcement no-claim；不创建真实 CI agent workflow |
| `check_external_harness_decisions.py` | `advisory` | manual / follow-up summary | 校验 `external-harness-decision/v1` 中 remote trace pilot、external eval/sandbox、MCP/A2A 与 CI agent workflow 四类 active decision 是否齐全，并强制 no hosted trace/eval、no verified remote without operator review、no native sandbox、no MCP/A2A runtime、no real CI agent workflow、no external effect without explicit confirmation；不发送网络 probe、不安装 eval 工具、不创建 MCP/A2A runtime、不创建 GitHub agent workflow |
| `check_agent_productization_readiness.py` | `review-required` | manual / follow-up summary | 校验 `agent-productization-readiness/v1` 模型和 `agent-productization-assessment/v1` 当前评估，固定 runtime orchestration、tool/MCP、memory、HITL、durability、trace、eval、sandbox、multi-agent、structured output、cost/latency、ops 12 个能力域；`partial` / `missing` / `deferred` 只输出 `REVIEW:`，不升级 blocking、不声明产品 agent 平台完成 |
| `check_config_contract.py` | `review-required` | manual / follow-up summary | 校验 `[config_contracts]` 声明的 registry、allowed paths、env template、secret key pattern、config key pattern 和 literal pattern；当前只做 repo-local 配置契约边界，不提供配置中心、secret manager、远端部署验证或 provider-specific 硬编码 |
| `check_env_template_sync.py` | `review-required` | manual; SessionStart hook warning-only | 只比较 env template 与本机 env 的 key 集合，不读取、不打印、不覆盖值；无 env template 配置时为 no-op；本机 `.env` 缺 key 只作为运行前提醒 |
| `check_mock_data_boundary.py` | `review-required` | manual / follow-up summary | 校验 `[mock_data_boundary]` 声明的 frontend/runtime scan roots、fixture paths、allowed consumers、runtime import denied paths、scenario manifest 和 inline 阈值；默认只输出 `REVIEW:` 且退出 0，`--strict` 需 burn-in 后显式启用；不自动删除旧代码、不安装 MSW/Prism/Playwright、不移动 mock 数据、不创建 API、不证明生产数据集成 |
| `run_sandboxed_command.py` | `advisory` | manual assistive wrapper | argv-only / `shell=false` / repo-root / reduced-env / timeout / runtime tool-output artifacts / preflight refusal；metadata 明示 `native_sandbox=false`，不能当作 OS sandbox、云端 execution engine 或完整 subprocess isolation |
| `check_tool_contracts.py` | `blocking-candidate` | governance job | MCP-like tools 或高影响工具 contract 增多后，根据误报率和 automation mode 使用情况升级 |
| `check_threejs_snake_contract.py` | `blocking` | governance job | 已强制；它是 WS-01 static contract gate，不替代 browser smoke |
| `collect_harness_sample_gaps.py` | `advisory` | governance job writes sample-gap markdown + JSON and appends the gap table to the step summary | 列出 security / guardrail / workflow 真实样本缺口，并从现有样本 checker 与通用 gap evidence ledger 汇总当前 evidence 计数；不自动生成证据，不升级 blocking |
| `check_harness_collection_config.py` | `advisory` | governance job validates collection routing config | 校验 roadmap gap id 与 collection config 的 dedicated target、priority、trigger、blocked / approved future-work target routing 和 review command 是否对齐；同时检查当前 readiness 实际输出的 capture gate / ledger action / readiness 是否都在 CLI choices 中，并要求每个真实采样 capture gate 都有 planner、template、intake、readiness、pending-focus focused command 覆盖，也要求每个真实采样 ledger action 都有 lane-wide planner、template、intake、pending-focus full-expansion command 覆盖，每个真实采样 readiness state 都有 planner、template、intake、readiness、pending-focus focused command 覆盖，每个 active real-sample area / priority 都有 planner、template、intake、readiness、pending-focus focused command 覆盖；active real-sample area / priority / ledger-action / capture-gate / readiness 还必须保留在 pending capture-focus choices 中，防止全量队列可见但聚焦过滤入口失效；已获 `sample_collection_allowed=true` 且 ADR refs 完整的 future-work gap 可配置 dedicated sample target，未批准的 future-work gap 仍必须走 future contract target；只防 queue/ledger 路由漂移，不生成 evidence、不接受 pending 样本、不升级 blocking |
| `plan_harness_sample_collection.py` | `advisory` | governance job writes collection queue markdown + JSON and appends the queue plus active-area, active-priority, append-new-pending-slot, fill-existing-placeholder, remote-interop, approved-bounded-incident, placeholder-replacement, security-workflow-event, bounded-real-incident, workflow-task-event, cross-task-resume, distinct-task-class-report, user-confirmed-high-impact-action, upgrade-decision-review, needs-first-real-sample, needs-more-real-samples, ready-for-upgrade-discussion, and local-sample-only capture-card focus to the step summary | 把当前缺口、readiness 计数、`readiness_metric_delta`、pending-slot 覆盖、pending review blockers、升级目标、ledger action 和 target checker command 转成下一批真实样本采集队列，并通过 `harness_sample_collection_render.py` 渲染 markdown / JSON / capture-card 输出；markdown queue 顶部汇总 priority / readiness / pending slot status / ledger action 分布，并显示 lane-specific review command 与 accepted-real/readiness metric delta；JSON / capture-card 明确输出 `readiness_metric_delta`、`replacement_review_command`、`append_review_command`、`outcome_review_command`、`upgrade_decision_review_command`、`contract_precondition_review_command`，避免消费者只靠 `ledger_action` 或 accepted real row count 推断复核命令 / readiness 是否满足；空过滤范围的 markdown / capture-card 会输出显式 no-match 并成功退出，避免把“当前没有该 lane”误读成工具失败或 accepted 证据；`review-upgrade-decision` entries 会把 lane review command 指向 `check_harness_upgrade_decision_candidate.py <candidate-jsonl>`，把 decision row 的 `next_evidence_needed` 暴露为 JSON / capture-card 的 `evidence_needed` / Next evidence needed，并让 upgrade decision template 复用已有 decision id，防止 ready gap 草稿被追加成 duplicate gap row；future-work contract-precondition entries 还会输出 `contract_blocker_state` 的 missing ADR refs、sample boundary、next action 和 review command，并把 lane review command 指向 `check_harness_future_work_contract_candidate.py <candidate-jsonl>`，让 blocked lane 可机器复核但不批准采样；已获 `approved-for-sampling` 且 ADR refs 完整的 future-work gap 会离开 contract-precondition lane，按 dedicated target 进入普通真实样本 append / pending review 路径；可按 area / priority / gap / ledger-action / capture-gate / readiness 聚焦，也可过滤 actionable without pending gaps 或 actionable without review-ready pending gaps，placeholder pending 不会从真实采集队列中移除；`--ledger-action append-new-pending-slot` 可看全部新增 pending lane，`--ledger-action fill-existing-placeholder` 可看全部占位替换 lane；`--capture-gate requires-approved-remote-interop` 可单独聚焦 ADR-017 允许的真实 remote interop probe lane，`--capture-gate requires-approved-bounded-incident` 可单独聚焦 ADR-016 允许的真实 bounded cascade-control incident lane，`--capture-gate replace-placeholder-after-real-event` 可单独聚焦等待真实 warning 后替换占位行的 lane，`--capture-gate requires-security-workflow-event` 可单独聚焦真实 PR / dependency / scheduled security workflow evidence lane，`--capture-gate requires-bounded-real-incident` 可单独聚焦真实 red-team incident lane，`--capture-gate requires-workflow-task-event` 可单独聚焦真实 workflow skill task lane，`--capture-gate requires-cross-task-resume` 可单独聚焦真实跨任务 resume lane，`--capture-gate requires-distinct-task-class-report` 可单独聚焦真实不同任务类 local trace summary report lane，`--capture-gate requires-user-confirmed-high-impact-action` 可单独聚焦需要显式用户确认的真实高影响动作 lane；`--ledger-action review-upgrade-decision` 可单独聚焦 ready gap 的 upgrade decision review lane；`--readiness needs-first-real-sample`、`--readiness needs-more-real-samples`、`--readiness ready-for-upgrade-discussion` 和 `--include-accepted --readiness local-sample-only` 可直接聚焦首个真实样本 blocker、已有 first sample 但仍未到 upgrade target 的 lane、ready-gap decision draft lane，或 local-only no-collection 边界，governance summary 也会单独展示 active area / active priority capture-card 视图、append-new-pending-slot、fill-existing-placeholder、remote interop、approved bounded incident、placeholder replacement、security workflow event、bounded real incident、workflow task event、cross-task resume、distinct task class report、user confirmed high impact action、upgrade decision review 和这四个 readiness capture-card 视图；`fill-existing-placeholder` 的 sample template 复用已有 pending sample id，提示替换占位行而不是追加新行；contract-precondition template 仅用于仍被阻塞的 future-work gap 并复用已有 contract id，提示替换合同前置行而不是追加重复 gap；`ready-for-upgrade-discussion` gap 显示 `review-upgrade-decision` 并路由到 `docs/ai/standards/harness-upgrade-decisions.jsonl`；local-only accepted gap 显示 `no-sample-collection`，不进入 append lane；local-sample-only capture-card 只做 inclusive 可见性，不生成样本草稿；只做计划，不采集证据、不授权高影响动作、不升级 blocking |
| `check_harness_burn_in_readiness.py` | `advisory` | governance job writes future/local-inclusive readiness markdown + JSON and appends full plus active-area / active-priority / real capture-gate / upgrade-decision-review / needs-first-real-sample / needs-more-real-samples / ready-for-upgrade-discussion / local-sample-only sections to the step summary | 汇总样本缺口的 accepted real / cross-task / local-only 计数，判断是否仍缺 first real sample、more real samples、contract/ADR 或只具备 local sample；输出 active area / priority / gap-id / capture-gate / readiness filters、area / priority / capture gate counts、`accepted_real_readiness_metric_deltas`、`readiness_gap_ids`、`capture_gate_gap_ids`、ready gap 的 `ready_next_evidence_needed_by_gap` 和 per-item `next_evidence_needed`，并在每个 gap 行内显示 `capture_gate` / `capture_gate_detail`，同时输出 target artifact、target checker command、`ledger_action`、lane-specific focused planner command、lane-specific focused intake command 和 lane review command；markdown 追加 `Next Collection Commands` 表和 `Ready Gap Next Evidence` 表，JSON 同步携带这些字段，让 readiness 审计与 planner / intake / pending focus 使用同一 roadmap bucket、真实事件前置条件、accepted-real/readiness metric 差异、exact remaining gap ids、keep-advisory 后续证据需求和 no-write review route，且不会把 ready-gap decision 或 approved future-work append lane 交接到通用空 intake scope；`--area` / `--priority` 可按 roadmap 维度聚焦，`--gap-id` 可只审计一个具体 gap，`--capture-gate` 可只审计一个真实事件或 review 门槛，例如 ADR-017 remote interop、ADR-016 approved bounded incident、placeholder replacement、security workflow event、bounded real incident、workflow task event、cross-task resume、distinct task-class report、user-confirmed high-impact action 或 upgrade-decision-review，`--readiness` 可只审计一个 readiness state，例如 `needs-first-real-sample`、`needs-more-real-samples`、`ready-for-upgrade-discussion` 或 `local-sample-only`，空过滤范围会显式返回 no-match 而不是 evidence 结论；governance summary 会追加 active area、active priority、所有真实采集 capture-gate、upgrade-decision-review、needs-first-real-sample、needs-more-real-samples、ready-for-upgrade-discussion 和 local-sample-only 聚焦视图，避免这些后排、roadmap bucket、priority bucket、真实事件前置条件、ready-gap decision review、局部 burn-in 缺口或 local-only no-collection 边界只藏在全量表格里；append / placeholder-fill / upgrade-decision / contract-precondition / local-only rows 只暴露下一步复核路径，不写 ledger、不生成或接受样本；ready gap 行内显示 upgrade decision 状态、decision ref、decision counts、missing decision 列表和当前决策账本的 `next_evidence_needed`，contract-blocked gap 行内复用 future-work `contract_states` 暴露 missing ADR refs、sample boundary 和 next action；已获 `sample_collection_allowed=true` 且 ADR refs 完整的 future-work gap 不再停在 contract/ADR readiness，而是进入 ordinary first-real-sample / more-samples readiness，避免 readiness 与 upgrade-decision / future-contract ledger 脱节；不生成 evidence、不升级 blocking、不替代 `check_harness_upgrade_decisions.py` 的严格校验 |
| `check_harness_upgrade_decisions.py` | `advisory` | governance job writes markdown + JSON decision report and appends it to the step summary | 校验所有 `ready-for-upgrade-discussion` gap 都有 bounded upgrade decision snapshot，并确认 source metric、accepted count 和 upgrade target 仍匹配当前 readiness；每条决策还必须提供存在的 repo-relative `evidence_refs` 和 `next_evidence_needed`，`evidence_refs` 可带 markdown anchor、pytest node id 或 JSONL 行号 selector 但底层路径必须存在，text / JSON 按 gap 暴露 keep-advisory 后续证据需求；当前 `GAP-WORKFLOW-TASK-PROFILE-AUDIT`、`GAP-AGENTIC-SANDBOX-HONESTY`、`GAP-GUARDRAIL-SOURCE-BOUNDARY` 与 `GAP-SEC-CONTROL-MATRIX-BURNIN` 决策均为 keep-advisory；不升级 blocking、不写 ADR、不生成样本 |
| `check_harness_upgrade_decision_candidate.py` | `advisory` | manual no-write upgrade decision candidate review | 校验单条 upgrade decision candidate 是否通过 schema、是否带有 `next_evidence_needed`、是否仍属于当前 `review-upgrade-decision` lane、readiness snapshot 是否仍匹配当前计数、是否复用已有 decision id 以替换当前行而不是追加 duplicate gap；报告会重新解析当前 queue 并显示 `ledger_action`、readiness、source metric、current / target、`capture_gate` / `capture_gate_detail`、`evidence_needed`、trigger、boundary、带 `--ledger-action review-upgrade-decision` 的 lane-specific focused planner command 和 focused intake command，让复核者确认该 ready gap 仍处于 upgrade-decision review scope，而不是相信 candidate 自述、手写 `--gap-id` 命令或运行通用空 intake scope；通过只说明可人工替换 upgrade decision 草稿，替换后仍需跑 `check_harness_upgrade_decisions.py`；不写 ledger、不写 ADR、不升级 blocking、不采集样本 |
| `check_harness_sample_templates.py` | `advisory` | governance job validates default, active-area, active-priority, append-new-pending-slot, fill-existing-placeholder, remote-interop, approved-bounded-incident, placeholder-replacement, security-workflow-event, bounded-real-incident, workflow-task-event, cross-task-resume, distinct-task-class-report, user-confirmed-high-impact-action, upgrade-decision-review, needs-first-real-sample, needs-more-real-samples, ready-for-upgrade-discussion generated drafts, and local-sample-only skip report | 校验 collection planner 生成的 pending JSONL 草稿、review-ready pending outcome candidate、upgrade-decision 草稿和 blocked future-work contract 草稿仍能被对应复核 gate 接受；`review-existing-pending-slot` 会从 pending ledger 原始行生成 outcome candidate 并通过 `check_harness_sample_outcome.py` 复核，避免把 outcome review lane 误当成普通 pending 模板；`review-upgrade-decision` 单条草稿通过 `check_harness_upgrade_decision_candidate.py` 复核，避免多个 ready gap 同时存在时单条模板被全账本 missing-decision 误报；已获采样批准的 future-work gap 会生成对应目标 ledger 的 pending 样本草稿；local-only `no-sample-collection` gap 会被跳过并报告 gap id，不生成 placeholder 样本草稿；输出 draft review-state counts、capture-gate counts 和 skipped no-sample-collection counts/gap ids，并在 JSON validation entry 中暴露 `template_review_state` / `template_review_blockers` / `capture_gate` / `capture_gate_detail`，防止 schema-valid 草稿被误读为 review-ready，也避免真实事件前置条件被隐藏；默认 `sampled_at` 使用运行当天，`--sampled-at` 可固定草稿日期用于复核；支持 area / priority / ledger-action / pending-state / capture-gate / readiness 聚焦过滤，便于复核和 intake bundle 同一草稿集，或只审计 `requires-approved-remote-interop`、`requires-approved-bounded-incident`、`replace-placeholder-after-real-event`、`requires-security-workflow-event`、`requires-bounded-real-incident`、`requires-workflow-task-event`、`requires-cross-task-resume`、`requires-distinct-task-class-report`、`requires-user-confirmed-high-impact-action` 等真实事件门槛，也可用 `--ledger-action append-new-pending-slot` / `--ledger-action fill-existing-placeholder` 分别审计新增 pending 与占位替换草稿，用 `--ledger-action review-upgrade-decision` 只审计 ready-gap upgrade decision drafts，或只审计 `needs-first-real-sample`、`needs-more-real-samples`、`ready-for-upgrade-discussion` 和 `local-sample-only` 等 readiness state；governance summary 会追加默认模板漂移报告、active area / active priority 模板报告、append-new-pending-slot 报告、fill-existing-placeholder 报告、remote interop 报告、approved bounded incident 报告、placeholder replacement 报告、security workflow event 报告、bounded real incident 报告、workflow task event 报告、cross-task resume 报告、distinct task class report 报告、user confirmed high impact action 报告、upgrade decision review 报告和这四个 readiness 聚焦报告；只防 schema / review-lane drift，不写 ledger、不采集真实样本、不批准 future-work sampling、不把模板计为 accepted burn-in |
| `build_harness_sample_intake_bundle.py` | `advisory` | governance job writes intake bundle plus default, active-area, active-priority, append-new-pending-slot, fill-existing-placeholder, remote-interop, approved-bounded-incident, placeholder-replacement, security-workflow-event, bounded-real-incident, workflow-task-event, cross-task-resume, distinct-task-class-report, user-confirmed-high-impact-action, upgrade-decision-review, needs-first-real-sample, needs-more-real-samples, and ready-for-upgrade-discussion compact summaries; manual assistive stdout bundle | 将 actionable without review-ready pending 的 pending JSONL 草稿按目标 artifact 分组，并在展示前复用目标 checker 校验；每条 entry 标注 target checker command、readiness source metric、current / target、`readiness_metric_delta`、`evidence_needed` capture checklist、pending slot status、ledger action、sample id、ledger refs、placeholder review blockers，以及 draft `template_review_state` / `template_review_blockers`，避免 schema-valid 模板或粗粒度 accepted real row 被误读为 review-ready、accepted 或 readiness-satisfied 证据；当 ledger action 是 `fill-existing-placeholder` 时，模板复用现有 placeholder id、显示 replace / do-not-append 写入模式，并在 text / summary / JSON 中指向 `check_harness_placeholder_replacement.py <candidate-jsonl>`，让真实事件补全后先做 no-write replacement review；当 ledger action 是 `append-new-pending-slot` 时，输出指向 `check_harness_sample_append.py <candidate-jsonl>`，让新增 pending 行前先做 no-write append review；已批准采样的 future-work gap 会走 append-new-pending-slot，而不是 contract-precondition；当 ledger action 是 `review-existing-pending-slot` 时，会从原 pending 行生成 outcome candidate、显示 outcome write mode，并通过 `outcome_review_command` 指向 `check_harness_sample_outcome.py <candidate-jsonl>`，让 pending outcome 变更先走 no-write outcome review；当 ledger action 是 `review-upgrade-decision` 时，text / summary / JSON 会通过 `upgrade_decision_review_command` 指向 `check_harness_upgrade_decision_candidate.py <candidate-jsonl>`，并在 Capture Checklist / Upgrade Decision Review 表里暴露 `next_evidence_needed`，只生成 upgrade decision 替换草稿，不生成 sample evidence，替换后仍需跑 `check_harness_upgrade_decisions.py`；当 ledger action 是 `define-contract-precondition` 时，text / summary / JSON 会通过 `contract_precondition_review_command` 指向 `check_harness_future_work_contract_candidate.py <candidate-jsonl>`，并输出 `contract_blocker_state` 的 missing ADR refs、sample boundary、next action 和 review command，只生成仍被阻塞的 future-work contract 前置替换草稿，不批准 sample collection，替换后仍需跑 `check_harness_future_work_contracts.py`；空过滤范围的 text / summary 会输出显式 no-match 并成功退出，避免把“当前没有该 intake lane”误读成工具失败或 accepted 证据；`--summary` 输出不含 JSONL 模板正文，可进入 GitHub step summary，并按 target 展示复核命令、readiness metric/current-target、readiness metric delta、readiness counts、Draft Template Review、Capture Checklist、Capture Gates、append/replacement/outcome/upgrade-decision/contract-precondition review sections、contract blocker state、ledger action counts 和 Pending Slot Blockers；默认 `sampled_at` 使用运行当天，`--sampled-at` 可固定草稿日期用于复核；支持 area / priority / gap / ledger-action / pending-state / capture-gate / readiness 聚焦，governance summary 会单独追加 active area / active priority `--summary` 视图、`--ledger-action append-new-pending-slot --summary`、`--ledger-action fill-existing-placeholder --summary`、`--capture-gate requires-approved-remote-interop --summary`、`--capture-gate requires-approved-bounded-incident --summary`、`--capture-gate replace-placeholder-after-real-event --summary`、`--capture-gate requires-security-workflow-event --summary`、`--capture-gate requires-bounded-real-incident --summary`、`--capture-gate requires-workflow-task-event --summary`、`--capture-gate requires-cross-task-resume --summary`、`--capture-gate requires-distinct-task-class-report --summary`、`--capture-gate requires-user-confirmed-high-impact-action --summary`、`--ledger-action review-upgrade-decision --summary`、`--readiness needs-first-real-sample --summary`、`--readiness needs-more-real-samples --summary` 与 `--readiness ready-for-upgrade-discussion --summary`；placeholder pending 仍需真实事件，会留在默认 bundle；stdout-only，不写 ledger、不接受样本、不批准 future-work sampling、不把模板计为 accepted burn-in |
| `check_harness_placeholder_replacement.py` | `advisory` | manual assistive no-write review gate | 校验一个完成后的 placeholder replacement JSON / JSONL 是否匹配已有 pending placeholder sample id、当前仍属于 `fill-existing-placeholder` lane、保持 `outcome=pending`、通过目标 sample checker，并从 placeholder 变成 review-ready；报告会从当前 queue 解析并显示 readiness、source metric、current / target、`capture_gate` / `capture_gate_detail`、`evidence_needed`、trigger 和 boundary，并回显该 gap 带 `--ledger-action fill-existing-placeholder` 的 lane-specific focused planner / intake command，让复核者在替换占位行前直接核对真实事件前置条件、精确 readiness 指标和当前采集上下文，而不是相信 candidate 自述或手写 `--gap-id` 命令；review-ready 复用 pending boundary blockers，防止候选写入前丢失 `no_external_claim`、`local_only` 或 `no_network` 边界；通过后会显示下一步 `check_harness_sample_outcome.py <candidate-jsonl>`，让 accepted / rejected outcome 仍走单独复核；它不写 ledger、不接受样本、不追加新行、不把候选样本计入 burn-in，只作为替换占位行前的确定性复核门 |
| `check_harness_sample_append.py` | `advisory` | manual assistive no-write review gate | 校验一个完成后的 append-new-pending-slot JSON / JSONL 是否仍属于当前 append lane、sample id 未在任何样本账本出现、保持 `outcome=pending`、通过目标 sample checker，并达到 review-ready；报告会从当前 queue 解析并显示 readiness、source metric、current / target、`capture_gate` / `capture_gate_detail`、`evidence_needed`、trigger 和 boundary，并回显该 gap 带 `--ledger-action append-new-pending-slot` 的 lane-specific focused planner / intake command，让复核者在 append 前直接核对真实事件前置条件、精确 readiness 指标和当前采集上下文，而不是相信 candidate 自述或手写 `--gap-id` 命令；review-ready 会拒绝 shared gap evidence 的 `no_external_claim` 漂移、real/local `local_only` 错配、red-team pending 边界漂移、local trace `no_network` 漂移，以及 Stage Checkpoint cross-task resume candidate 仍沿用模板 checkpoint id 或未标注 `resume_scope=cross-task`；通过后会显示下一步 `check_harness_sample_outcome.py <candidate-jsonl>`，让 accepted / rejected outcome 仍走单独复核；它不写 ledger、不接受样本、不替换占位行、不把候选样本计入 burn-in，只作为新增 pending 行前的确定性复核门 |
| `check_harness_sample_outcome.py` | `advisory` | manual assistive no-write review gate | 校验一个完成后的 outcome-change JSON / JSONL 是否匹配已有 review-ready pending sample id、当前仍属于 `review-existing-pending-slot` lane、只把 `outcome` 改成 `accepted` 或 `rejected`、保持 schema / gap / source_type 对齐、边界字段不漂移，并通过目标 sample checker；报告会从当前 queue 解析并显示 `ledger_action`、readiness、source metric、current / target、`capture_gate` / `capture_gate_detail`、`evidence_needed`、trigger、boundary、带 `--ledger-action review-existing-pending-slot` 的 lane-specific focused planner command 和带 `--pending-state with-review-ready-pending` 的 focused intake command，让复核者在改 outcome 前确认该 pending row 仍处于 outcome-review scope，而不是相信 candidate 自述或手写 `--gap-id` 命令；它会读取目标 pending ledger 原始行，只允许 outcome / review 字段变化，拒绝 `sample_summary`、`sampled_at`、`boundary_note` 等稳定 evidence 字段被 outcome candidate 改写；它拒绝直接把 placeholder pending 改成 accepted，也拒绝 outcome candidate 中的 `no_external_claim`、`local_only` 或 `no_network` 边界漂移；不写 ledger、不追加 evidence、不替换占位行，只作为人工改 outcome 前的确定性复核门；只有通过复核且 `accepted` + `real` 的候选才会显示 `burn_in_counted=true` |
| `check_harness_pending_samples.py` | `advisory` | governance job writes future/local-inclusive markdown + JSON and appends lane report, default/full-expansion/active-area/active-priority/append-new-pending-slot/fill-existing-placeholder/remote-interop/approved-bounded-incident/placeholder-replacement/security-workflow-event/bounded-real-incident/workflow-task-event/cross-task-resume/distinct-task-class-report/user-confirmed-high-impact-action/needs-first-real-sample/needs-more-real-samples capture focus cards, and placeholder review cards to the step summary | 汇总各 JSONL 样本账本的 accepted / pending / rejected 状态，并按 `real` / `synthetic` / `local-replay` / `local-only` 拆分 accepted 计数后对齐当前 collection queue；pending 槽位会标成 `review-ready` 或 `placeholder`，并输出 placeholder review blockers、ledger action gaps、actionable without review-ready pending、next collection lane commands 和 bounded `next_capture_focus` 聚焦命令，避免占位行掩盖仍需真实事件，也避免人工从完整队列里猜下一步；`--capture-focus` 会把这些聚焦项渲染成 compact read-only cards，带 roadmap area、target artifact、target checker、带当前 `ledger_action` 的 lane-specific planner / intake / lane review、`capture_gate` / `capture_gate_detail`、readiness、readiness metric delta（如有）、evidence needed checklist、pending refs、pending blockers、trigger、boundary、active area / priority / ledger-action / capture-gate / readiness filters、shown/available focus count、area / priority / ledger-action / capture-gate / readiness bucket counts、limit 和 truncated 状态；默认只列前 5 个，governance workflow 还会追加 `--capture-focus-limit 0` 全量 matching lane 视图、active area / active priority 全量聚焦视图、`--capture-focus-ledger-action append-new-pending-slot --capture-focus-limit 0` 新 pending lane 视图、`--capture-focus-ledger-action fill-existing-placeholder --capture-focus-limit 0` 占位替换 lane 视图、`--capture-focus-gate requires-approved-remote-interop` ADR-017 remote interop 视图、`--capture-focus-gate requires-approved-bounded-incident` ADR-016 bounded cascade incident 视图、`--capture-focus-gate replace-placeholder-after-real-event` 占位替换视图、`--capture-focus-gate requires-security-workflow-event` 安全证据视图、`--capture-focus-gate requires-bounded-real-incident` red-team 真实 incident 视图、`--capture-focus-gate requires-workflow-task-event` workflow skill 任务视图、`--capture-focus-gate requires-cross-task-resume` 跨任务 resume 视图、`--capture-focus-gate requires-distinct-task-class-report` 不同任务类 local trace 视图、`--capture-focus-gate requires-user-confirmed-high-impact-action` 显式用户确认高影响动作视图、`--capture-focus-readiness needs-first-real-sample` 首样本 blocker 视图和 `--capture-focus-readiness needs-more-real-samples` 局部 burn-in 视图，避免 P2/P3、后排 area、priority bucket、append lane、placeholder replacement、remote interop、approved bounded incident、security evidence、red-team incident、workflow task event、cross-task resume、distinct task class report、user confirmed high impact action、first-sample blocker 或 more-sample lane 只藏在 bucket count 里；`--capture-focus-area`、`--capture-focus-priority`、`--capture-focus-ledger-action`、`--capture-focus-gate` 和 `--capture-focus-readiness` 只过滤 next-capture 聚焦卡，不改变完整 pending accounting 或 queue totals；JSON 同步输出 `next_capture_focus_area_filter`、`next_capture_focus_priority_filter`、`next_capture_focus_ledger_action_filter`、`next_capture_focus_capture_gate_filter`、`next_capture_focus_readiness_filter`、`next_capture_focus_count`、`next_capture_focus_available_count`、`next_capture_focus_limit`、`next_capture_focus_truncated` 以及 shown/available area / priority / ledger-action / capture-gate / readiness counts，并在每个 `next_capture_focus` entry 带 `area`、`capture_gate`、`capture_gate_detail`、`readiness`、`readiness_metric_delta`、`evidence_needed`、`pending_slot_refs` 和 `pending_review_blockers`，让采集者不必先打开 intake bundle 或 review cards 才知道 roadmap area、真实事件前置条件、readiness 状态、账本 accepted real 粗计与精确 source metric 的差异、bounded evidence 字段、既有占位 sample id / ledger ref 和 blockers，也不会拿到退回通用 `--gap-id` scope 的 planner / intake 命令；review blockers 会把 shared gap evidence 的 `no_external_claim`、real/local `local_only`、red-team pending `local_only/no_external_claim`、local trace `no_network/local_only` 和 Stage Checkpoint cross-task resume 模板 checkpoint 未替换视为 placeholder；`fill-existing-placeholder` lane 和 placeholder review cards 会指向 `check_harness_placeholder_replacement.py <candidate-jsonl>`，让真实事件补全后先做 no-write replacement review；`append-new-pending-slot` lane 会指向 `check_harness_sample_append.py <candidate-jsonl>`，让新增 pending 行前先做 no-write append review；`review-existing-pending-slot` lane 和 review-ready cards 会指向 `check_harness_sample_outcome.py <candidate-jsonl>`，让 pending 行改成 accepted / rejected 前先走 no-write outcome review；`review-upgrade-decision` lane 会指向 focused planner / intake bundle、`check_harness_upgrade_decision_candidate.py <candidate-jsonl>` 和 `check_harness_upgrade_decisions.py`，让 ready gap 先复核 keep/promote/defer 决策候选再做全账本审计；local-only accepted gap 显示 `no-sample-collection`，不输出下一步采样命令；在 stdout/JSON 中显式区分 actionable sample gaps、contract-blocked gaps、ready upgrade-decision gaps、ready upgrade-decision next evidence 和 local-only gaps，并通过 `ready_upgrade_decision_next_evidence_by_gap` 暴露 keep-advisory 后续证据需求，通过 `contract_blocker_states` 暴露仍被阻塞的 future-work missing ADR refs、sample boundary、next action 和 review command；已批准采样的 future-work gap 计入 actionable sample gaps，不再列入 contract-blocked；普通 text 输出和 `--review-cards` 在空过滤范围都会显式 no-match 并成功退出，避免把空 scope 当成 evidence 结论或工具失败；`--review-cards` 可配合 `--gap-id` / `--review-state` 聚焦待复核槽位，把 pending 槽位绑定到 checker / evidence class / review state / review blockers / ledger action / capture gate / trigger / evidence checklist / replacement 或 outcome review command / readiness / 边界说明，不生成样本、不接受样本、不批准 future-work sampling、不升级 blocking |
| `check_harness_future_work_contracts.py` | `advisory` | governance job writes future-work contract markdown + JSON and appends the contract preconditions to the step summary | 校验 future-work gaps 是否有 contract 前置条件，并在 JSON `contract_states` 中按 gap 暴露 ADR refs、`missing_adr_refs`、sample collection boundary、next action 和 review command；`approved-for-sampling` 必须引用存在的已采纳 repo ADR，且 ADR 需覆盖 `gap_id`、contract id、`auth_model`、`endpoint_or_authority_scope`、`redaction_or_boundary_model` 和 `cost_or_stop_boundary`；保证未获 ADR / contract approval 前不能开启 sample collection，获批后也只代表 planner 可以按边界生成 pending 样本草稿；不证明远端互通、不采集样本、不升级 blocking |
| `check_harness_future_work_contract_candidate.py` | `advisory` | manual no-write future-work contract candidate review | 校验单条仍处于 `define-contract-precondition` lane 的 future-work contract candidate 是否通过 schema、是否复用已有 contract id 以替换当前行而不是追加 duplicate gap；报告会重新解析当前 queue 并显示 `ledger_action`、readiness、source metric、current / target、`capture_gate` / `capture_gate_detail`、`evidence_needed`、trigger、boundary、按当前 `ledger_action` 生成的 lane-specific focused planner command 和 focused intake command，让复核者确认该 future-work gap 仍处于 contract-precondition scope，或看清已获批合同当前已路由到 append lane，避免继续使用通用空 intake scope；candidate 进入 `approved-for-sampling` 时同样必须通过已采纳 ADR 引用和 contract coverage 校验；通过只说明可人工替换前置合同草稿，替换后仍需跑 `check_harness_future_work_contracts.py`；已获批并离开 contract-precondition lane 的合同由全量 checker 继续审计，不再通过 candidate gate 追加同 gap 行；不写 ledger、不批准 future-work sampling、不采集样本、不证明远端互通 |
| `check_workspace_sandbox.py` | `blocking-candidate` | governance job | 本地 sandbox / rehydration manifest 能稳定防止过度声明后，再决定是否阻断；该检查不证明 native sandbox provider、OpenAI sandbox、MCP/A2A 或外部 OTLP 互通 |
| `check_agentic_red_team_samples.py` | `advisory` | governance job validates red-team sample artifact; real incidents remain burn-in | 校验 `agentic-red-team-sample/v1` JSONL 的 AC control 映射、risk family、local-replay / real-incident 类型、false-positive rule、replay command、存在的 repo-relative `evidence_refs`（允许 selector）和 raw runtime 边界；当前有 8 个 accepted local-replay 样本覆盖全部 8 类 red-team risk family，并有 2 个 `sandbox-claim-honesty` accepted real incidents；其它 red-team risk family 的 real incident 仍需 burn-in |
| `check_harness_sample_gap_evidence.py` | `advisory` | governance job validates generic gap evidence artifact; real samples remain burn-in | 校验 `harness-sample-gap-evidence/v1` JSONL 的 source_type、outcome、本地/外部声明边界、OTLP local pilot / remote interop 约束、存在的 repo-relative `evidence_refs` 和 raw runtime 边界；`evidence_refs` 可带 markdown anchor、pytest node id 或 JSONL 行号 selector，但底层路径必须存在；当前 `GAP-TRACE-OTLP-PILOT-BURNIN` 有 1 个 accepted local-interop sample，`GAP-GUARDRAIL-SOURCE-BOUNDARY` 有 2 个 accepted real source-boundary samples 且升级决策为 keep-advisory，`GAP-SEC-CONTROL-MATRIX-BURNIN` 有 2 个 accepted real AC-01 mapping samples 且升级决策为 keep-advisory，`GAP-TRACE-REMOTE-INTEROP` 已可接收 `real-interop-run` pending 样本但 accepted real remote interop sample 仍为 0 |
| `check_harness_sample_followup_coverage.py` | `advisory` | governance job writes coverage markdown + JSON and appends the audit to the step summary | 发现 sample-gap control-plane docs / scripts / tests、governance workflow sample summary、template record helper、shared review context helper 与 readiness routing helper，并确认每个文件变更都会触发 `harness-sample-gap-evidence` follow-up，同时要求 `REQUIRED_COMMANDS` 与 routed `HARNESS_SAMPLE_GAP_COMMANDS` 完全闭合；新增 routed command 未进入 required coverage、required command 未进入 routed follow-up、或 collection config、queue、empty-scope regression、unfiltered intake/readiness baseline、intake JSON output、future/local-inclusive state、pending、capture-focus default / full-expansion / active area / active priority / lane / capture-gate / readiness filter、readiness next-collection routing、local-sample-only template skip、replacement、append、outcome、future-work contract、workflow-output test、unit test、coverage audit 命令缺失都会报错；workflow-output test 还会按当前 active real-sample area / priority / capture gate / ledger-action / readiness 推导 GitHub step summary 应包含的 planner、template、intake、readiness、pending-focus 命令和对应 summary section / `cat /tmp/...` 输出，防止 follow-up 命令包完整但 CI summary 漏掉新 bucket 或 lane 可见段；只防路由漂移，不证明命令已运行、不生成 evidence、不升级 blocking |
| `check_burn_in_ledger.py` | `advisory` | governance job writes burn-in ledger markdown + JSON and appends the ledger audit to the step summary | 校验 blocking-candidate checks 是否都有 burn-in ledger 行，并防止 `0/2` 或 `1/2` 样本提前标记为 `ready-for-adr` / `promote-to-blocking`；markdown / JSON 现在输出 decision counts、remaining sample slots、checks needing samples、upgrade-eligible checks、upgrade-review-needed checks，以及每个 check 的 accepted / target / remaining、evidence refs、current decision、next evidence、repair path、cost、false-positive note、upgrade eligibility 和 upgrade-review-needed state；accepted sample 大于 0 时必须有存在的 repo-relative evidence refs，允许 markdown anchor、pytest node id 或 JSONL 行号 selector 但底层路径必须存在；`2/2` keep-candidate 行必须把 next evidence 指向 upgrade decision review，便于升级校准不再人工解析表格或接受不可审计数字；它不生成样本，也不自动授权升级 |
| `check_burn_in_upgrade_decisions.py` | `advisory` | governance job validates check-level upgrade decision ledger | 校验 `docs/ai/standards/check-burn-in-upgrade-decisions.jsonl` 是否覆盖所有 `upgrade_review_needed_checks`，并防止 stale accepted sample / target / current decision snapshot、duplicate check、缺 next evidence、不可审计 evidence refs 或引用 `.codex/runtime` 原始材料；`evidence_refs` 允许 markdown anchor、pytest node id 或 JSONL 行号 selector 但底层路径必须存在；当前 `check_code_shape.py` 与 `check_tool_contracts.py` 均已有 keep-candidate 决策记录；该检查只审计决策，不改 check level、不写 ADR、不采集样本、不升级 blocking |
| `check_warning_sample_code_alignment.py` | `advisory` | governance job validates hook/checker warning-code alignment | 校验 PreToolUse preflight 和 Stop loop/scope hook 实际 emitted finding codes、导出的 code 清单、样本 checker `FINDING_CODES` 对齐，并确认 Stop recommendation mapping 覆盖所有 finding 且 action code 被样本 checker 接受；只防 code/schema 漂移，不生成 evidence、不接受 pending 样本、不升级 blocking |
| `check_change_triggered_followups.py` | `advisory` | PR / main push summary；包含 high-impact-agent-actions review-required advisory | 不直接升级；只驱动其他 checks 与人工确认 |
| `check_context_budget.py` | `blocking` | governance job / pre-commit / `check_ai_governance.py`; Stop hook inherits through governance check | 默认面、skill catalog、raw source、static task packet 达到 90% 压缩触发线或硬预算超限时阻断；`--warning-only` 仅保留人工审计输出 |
| `check_prototype_design_brief.py` | `blocking` when enabled | `check_ai_governance.py` only when `[prototype_design_brief].enabled = true`; manual otherwise | Prototype Design Brief 是受控设计投影面；开启后缺必填章节、关键语义门槛、Source Truth 未绑定或 REQ/WS/ADR/link 漂移时阻断 |
| `check_prototype_artifact_review.py` | `blocking` when enabled | `check_ai_governance.py` only when `[prototype_design_brief].artifact_review_enabled = true`; manual otherwise | Prototype artifact package 和配置的静态原型缺审查包、route、关键状态、truth boundary、surface identity、non-production boundary 或工具无关性时阻断 |
| `check_runtime_token_budget.py` | `blocking-candidate` | governance job validates no-transcript wiring; manual transcript audit uses `--transcript <rollout-jsonl>` and optional `--strict`; Stop token-pressure hook reuses the thresholds for warning-only current-transcript summaries | 两次以上真实长会话样本证明阈值、误报率和修复路径可控后，才考虑是否把 runtime transcript audit 升级为阻断；不得把本机历史 transcript 作为默认阻断输入 |
| `pre_tool_use_preflight.py` | `advisory` | PreToolUse hook warning-only; unit tests cover large-output, destructive, external-send, benign-command paths, finding-code output, and bounded sample-capture guidance; sample checker validates burn-in artifact | 只提醒动作前风险，不输出 `continue: false`，不等同用户授权；warning 会显示 finding codes 和 placeholder replacement gate，方便真实 warning 后填 bounded sample；若未来考虑阻断，必须先记录真实样本、误报率和任务中断成本 |
| `check_pre_tool_use_preflight_samples.py` | `advisory` | governance job validates PreToolUse preflight sample artifact; accepted real warning samples remain burn-in | 校验 `pre-tool-use-preflight-sample/v1` JSONL 的 finding / operator decision、outcome、false_positive、action_taken、存在的 repo-relative `evidence_refs`（允许 selector）和 raw tool 边界；synthetic 样本不计入真实 burn-in，当前 accepted real warning sample 为 0 |
| `stop_runtime_token_pressure.py` | `advisory` | Stop hook warning-only; bounded `additionalContext` is covered by unit tests | 只提醒下一轮 checkpoint / 新 session / raw artifact summary，不输出 `continue: false`；若未来考虑阻断，必须先记录真实样本、误报率和用户中断成本 |
| `stop_loop_scope_monitor.py` | `advisory` | Stop hook warning-only; unit tests cover repeated tool commands, repeated failed tool outputs, excessive validation/test loops, possible task-scope churn, finding/recommendation-code output, sample-capture guidance, and bounded `additionalContext`; sample checker validates burn-in artifact | 只提醒下一轮 checkpoint / 新 session / 缩小验证面，不输出 `continue: false`，不自动 compact，不自动归档；warning 会显示 finding / recommendation codes 和 placeholder replacement gate，方便真实长会话 warning 后填 bounded sample；若未来考虑阻断，必须先记录真实长会话样本、误报率和任务中断成本 |
| `check_loop_scope_monitor_samples.py` | `advisory` | governance job validates loop/scope monitor sample artifact; accepted real warning samples remain burn-in | 校验 `loop-scope-monitor-sample/v1` JSONL 的 finding / recommendation、outcome、false_positive、action_taken、存在的 repo-relative `evidence_refs`（允许 selector）和 raw runtime 边界；synthetic 样本不计入真实 burn-in，当前 accepted real warning sample 为 0 |
| `check_stage_checkpoints.py` | `advisory` | governance job validates checkpoint artifact and resume samples; cross-task usefulness remains burn-in | 校验 `stage-checkpoint/v1` 与 `stage-checkpoint-resume-sample/v1` JSONL 的 stage/status、恢复提示、下一步、evidence、REQ/WS、`resume_scope`、存在的 repo-relative resume sample `evidence_refs`（允许 selector）和 raw runtime 边界；不做 execution engine，不自动写 canonical docs，不允许 raw transcript / prompt / `.codex/runtime/*` 进入 checkpoint；当前 2/2 accepted samples、0 个 accepted cross-task sample，只证明同一 harness-hardening 线程内有用，保持 advisory；新增 pending cross-task 样本还需通过 append gate，不能沿用模板里的 harness-hardening checkpoint id |
| `check_runtime_execution_snapshots.py` | `advisory` | governance job validates local execution snapshot artifacts | 校验 `runtime-execution-snapshot/v1` 的 state、authority、tool contract 绑定和 local-only claim；它只证明 bounded checkpoint / resume 原料存在，不等于通用 execution engine |
| `summarize_runtime_traces.py` | `advisory` | governance job runs no-network summary smoke; unit tests cover local observation and agent-trace summaries; sample checker validates no-network report evidence | 只读 `.codex/runtime/observations/*.jsonl` 与 `agent-traces/*.agent-trace.jsonl`，输出本地 Markdown / JSON 摘要；不上传、不阻断、不声明 OpenAI / OTLP / MCP / A2A 互通；当前有 3 个 accepted real local JSON report 样本，但 accepted distinct task class 仍只有 1 个（`harness-hardening`），若未来考虑升级，必须先记录更多真实 task class、误报率、redaction 边界和治理 promotion 修复路径 |
| `summarize_harness_capabilities.py` | `advisory` | governance job appends compact capability summary to the step summary | 只压缩 durability / interop / task-eval / high-impact guardrail coverage；不生成新证据，不升级 control level |
| `summarize_loop_triage.py` | `advisory` | manual / follow-up summary | 只读 capability summary 与 sample collection queue，把现有信号排序成 operator-reviewed next-action candidates；不写 ledger、不收集样本、不升级 blocking、不执行修复、不声明 scheduler / MCP / A2A / hosted eval / native sandbox / CI agent runtime |
| `check_local_trace_summary_samples.py` | `advisory` | governance job validates Local Trace Summary sample artifact; external interop remains future work | 校验 `local-trace-summary-sample/v1` JSONL 的 no-network/local-only、task_class、计数、redaction state、存在的 repo-relative `evidence_refs`（允许 selector）和 raw runtime 边界；accepted real task class 只证明本地 summary 对该类任务有用，不证明外部 tracing 互通 |
| `summarize_tool_output.py` | `advisory` | supporting tool for `check_runtime_token_budget.py`; no standalone CI gate | raw artifact 留在 `.codex/runtime/tool-outputs/`，transcript 只进入摘要、错误匹配、tail 和行窗；不单独升级 blocking |
| `check_task_profile_audit.py` | `advisory` | governance job validates sample artifact; real task audits remain manual / burn-in | 校验 task profile audit JSONL 中的 profile、实际读取面、改动文件、验证命令、traceability closure、real/synthetic 样本类型、存在的 repo-relative `evidence_refs`（允许 selector）、accepted real profile 计数和 raw runtime 边界；当前已有 accepted real simple / complex / 0-1-stage 样本各 1 个，达到升级讨论的 profile 覆盖门槛；升级前必须继续记录更多真实样本、误报率和流程税，并单独决策 |
| `check_archive_candidates.py` | `advisory` | manual | 不自动归档；保持主 Agent 语义判断 |
| `check_skill_usage_samples.py` | `advisory` | manual | 只证明 skill 样本是否足够，不阻断业务 |
| `check_repo_skills.py` | `review-required` | manual / starter validation | skill 结构频繁变更后再考虑 CI |
| `check_skill_catalog.py` | `review-required` | governance job / starter validation; red-team sample checker records local replay evidence for tool-output injection and skill squatting | `.codex/skills` vendor/proxy/lock 与 `--check-output` 真实第三方样本证明误报可控后再考虑升级为 blocking；当前 local replay 不等同真实 dependency incident |
| Scorecard / CodeQL / SBOM / dependency review / secret scanning advisory | `advisory` | single `security-evidence` job with artifacts; dependency review PR job; secret scanning only when external evidence or plan supports it; 首轮 PR + main push evidence 已通过，CodeQL code-scanning 上传注解按 triage 处理 | 按 `docs/ai/security/security-evidence-triage.md` burn-in 后依据严重度、误报率、owner 和修复路径逐项升级 |
| `agentic-control-matrix.md` | `review-required` | manual / security review evidence | 只做 OWASP / NIST 风格控制映射；真实样本、owner、误报率和修复路径满足后才推动具体 check 升级 |

## 最新口径补充

- 2026-06-16：新增 Coding Agent / Browser Harness Selection 标准，并把 external eval/sandbox 与 MCP/A2A active decision 补充为 source-backed selection policy。当前 coding-agent comparator 默认优先 `mini-swe-agent`，SWE-agent 主仓库保留为历史 / SWE-bench / trajectory 参考；deterministic browser smoke、CI gate 和 local static checks 默认优先 repo script + CLI / skills，MCP 只在 persistent state、rich introspection 或显式 MCP interop 需要时评估。该标准只改变比较与 transport 默认选择，不新增依赖、不创建 MCP/A2A runtime、不声明 native sandbox、hosted eval/trace 或真实 CI agent workflow。
- 2026-06-16：新增并增强 Mock Data Boundary 标准、`[mock_data_boundary]` 配置和 `check_mock_data_boundary.py`。该机制用于早期发现产品页面/组件中的超大 inline mock、生成式 mock、runtime mock/fixture import、未登记大型 fixture 和 unseeded fixture factory，并把大型样例数据收敛到声明的 scenario manifest / fixture / network-handler surface；默认 review-required / non-blocking，不自动清理旧代码或声明生产数据接入完成。
- 2026-06-14：新增 Enterprise Code Boundary Candidate skill 与三份 review-required 标准，首批覆盖 logging/redaction、error contract 和 runtime side effect；它只做规范索引和复核路由，不新增 checker、不升级 blocking、不声明生产观测或远端副作用治理完成。
- 2026-06-14：新增 Config Contract Boundary 标准、`check_config_contract.py`、`check_env_template_sync.py` 和 SessionStart env template drift warning。该机制只约束 repo 内配置 registry / template / scanned code 的边界，不读取或输出 env 值，不接生产配置中心、secret manager 或远端部署验证；provider-specific 规则必须进入 `[config_contracts]`，不得硬编码在通用 checker 中。
- 2026-06-12：新增 Agent Productization Readiness 标准和 `check_agent_productization_readiness.py`。该检查把成熟产品 agent 的 12 个能力域固化为 review-required readiness model，并记录当前 `ai-ms-pro-harness-control-plane` 的 partial/deferred 短板；它只做缺口雷达，不新增外部依赖、不接 hosted trace/eval、不创建 MCP/A2A runtime 或 CI agent workflow，也不改变 Stage-00 local-first 边界。
- 2026-06-12：新增 evidence-based coding standards 和 `$repo-governed-coding` checklist。该标准是 review-required，不改变 Ruff blocking scope，不启用 JS/TS lint；候选自动化规则包括 Ruff `PLR2004`、`C901`、`PLR0912`、`PLR0915` 和未来 JS/TS `no-magic-numbers`，都必须先收集真实样本、误报率、修复路径、CI 成本和 reviewer 负担。
- 2026-06-08：新增 opt-in Prototype Design Brief 与 prototype artifact review 机制。默认 `[prototype_design_brief]` 关闭；开启后由 `check_ai_governance.py` 追加 brief / artifact child checks，只验证设计投影面与原型审查包，不复制其他项目业务 truth，也不声明当前 repo 已有生产原型能力。
- 2026-06-07：新增 `external-harness-decision/v1` 决策账本和 `check_external_harness_decisions.py`，把 remote trace pilot、external eval/sandbox、MCP/A2A、CI agent workflow 四个先前需要人工判断的方向转为 bounded active decisions。当前决策是：remote trace 等待显式 endpoint/`--send`/operator review，external eval/sandbox 先 comparison-only 且不加依赖，MCP/A2A 只做 contract registry，CI agent 保持 advisory、不创建真实 workflow。
- 2026-06-06：五个 harness 反哺点进入 bounded 小切片：remote interop checker 现在拒绝 localhost / loopback verified-remote，CI agent contract 与 local execution policy wrapper 登记为 advisory，planner / executor / reviewer 只作为 schema sample，cross-task resume 仍需真实非 harness 任务样本。该批更新不升级 blocking、不声明 hosted/cloud agent、native sandbox、A2A 或 verified remote completion。
- 2026-05-25：`check_harness_pending_samples.py` 现在在 text / JSON 输出 `ready_upgrade_decision_next_evidence_by_gap`，把 ready gap 的 keep-advisory 后续证据需求放到 pending audit 旁路可见；该输出只读当前 collection queue / upgrade-decision snapshot，不写 ledger、不生成样本、不接受 pending row。
- 2026-05-25：`check_harness_burn_in_readiness.py` 现在从 `docs/ai/standards/harness-upgrade-decisions.jsonl` 读取 ready gap 的 `next_evidence_needed`，并在 text / JSON 输出 `ready_next_evidence_needed_by_gap` 与 per-item `next_evidence_needed`；该输出只暴露 keep-advisory 后续证据需求，不写 ledger、不生成样本、不升级 blocking。
- 2026-05-25：`check_harness_upgrade_decisions.py` 现在要求 ready-gap upgrade decision 的 `evidence_refs` 是存在的 repo-relative 路径，允许 markdown anchor、pytest node id 和 JSONL 行号 selector，并继续拒绝 `.codex/runtime` 原始材料；该校验只防决策证据引用漂移，不新增样本、不改等级。
- 2026-05-25：`check_harness_sample_gap_evidence.py` 现在也要求通用 gap evidence ledger 的 `evidence_refs` 指向存在的 repo-relative 路径；markdown anchor、pytest node id 和 JSONL 行号 selector 可以保留，但底层文件必须存在。该校验只防下一批 pending / accepted 样本引用漂移，不生成或接受样本。
- 2026-05-25：red-team、PreToolUse preflight、loop/scope monitor、Local Trace Summary、Task Profile Audit 和 Stage Checkpoint resume sample checker 现在同样要求 `evidence_refs` 指向存在的 repo-relative 文件，并允许 selector；该校验只防独立样本账本引用不可审计证据，不生成样本、不改变 readiness 或升级决策。
- 2026-05-25：`check_harness_upgrade_decisions.py`、`check_burn_in_ledger.py` 与 `check_burn_in_upgrade_decisions.py` 现在复用共享 evidence-ref helper，ready-gap decision、check-level burn-in ledger 和 check-level upgrade decision ledger 的 `evidence_refs` 同样允许 markdown anchor、pytest node id 和 JSONL 行号 selector，但底层路径必须存在；`scripts/evidence_ref_utils.py` 改动也会触发这些 checker 的 follow-up。该校验只防升级证据引用漂移，不生成样本、不升级 blocking。
- 2026-05-25：`check_harness_burn_in_readiness.py` 现在输出 `accepted_real_readiness_metric_deltas`，把账本 accepted real 粗计与具体 readiness source metric 的差异直接列出来；例如 Stage Checkpoint real ledger row 不等于 cross-task resume 通过，Local Trace raw report row 不等于 distinct task-class 覆盖。该输出只防误读，不写 ledger、不改变 readiness 判定。
- 2026-05-25：`check_harness_pending_samples.py` 的 JSON 现在输出 `queued_readiness_metrics_by_gap` 与 `accepted_real_readiness_metric_deltas`；text 输出显示 queued readiness metric row count 和 accepted-real/readiness 差异，`--capture-focus` 卡片显示 `Metric`、`Current / target` 和单卡 `Readiness metric delta`（如有）。该口径用于防止把账本级 `accepted_real_by_gap` 粗计误读成 readiness 通过；升级讨论仍以 `check_harness_burn_in_readiness.py` 的 source metric 为准。
- 2026-05-25：`plan_harness_sample_collection.py` 和 `build_harness_sample_intake_bundle.py` 现在在 queue / capture-card / intake summary / JSON 中带 `readiness_metric_delta`，让 Stage Checkpoint 与 Local Trace 这类 accepted-real/readiness source metric 差异在 planner 与 intake handoff 面也不会丢失；该输出只补齐可见性，不写 ledger、不生成或接受样本。
- 2026-05-25：`check_burn_in_ledger.py` 现在把 blocking-candidate burn-in ledger 的每行样本进度、evidence refs、剩余样本、当前决策、next evidence 和 upgrade-review-needed state 结构化输出到 markdown / JSON，并汇总 `total_remaining_samples`、`checks_needing_samples`、`upgrade_eligible_checks` 与 `upgrade_review_needed_checks`；accepted sample 大于 0 时必须有存在的 repo-relative evidence refs，`2/2` keep-candidate 行必须把 next evidence 指向 upgrade decision review。当前 `check_code_shape.py` 与 `check_tool_contracts.py` 均已记录 2/2 accepted samples 并进入 upgrade-eligible / upgrade-review-needed，二者仍保持 `keep-candidate`；剩余真实样本槽位为 10。该输出只辅助升级校准，不生成样本、不升级 blocking。
- 2026-05-25：`check_burn_in_upgrade_decisions.py` 现在校验 check-level upgrade decision ledger；当前 `check_code_shape.py` 与 `check_tool_contracts.py` 的 `2/2` 样本达标状态均已有 bounded keep-candidate 决策，下一步分别是更多非 harness changed-file 样本、更多 MCP-like / high-impact tool contract 样本，而不是直接升级 blocking。
- 2026-05-25：`check_harness_pending_samples.py` 的 `next_capture_focus` entries/cards 现在对 placeholder replacement lane 直接显示 pending refs 与 pending blockers，避免采集者为了知道要替换哪一行再打开 `--review-cards`。
- 2026-05-25：`check_harness_pending_samples.py` 的默认 `next_capture_focus` 截断输出现在同步暴露 `next_capture_focus_hidden_gap_ids`；普通 text 和 `--capture-focus` cards 都会列出 hidden gap ids，避免只从 bucket counts 猜后排采集 lane。
- 2026-05-25：`check_harness_burn_in_readiness.py` 的 summary/JSON 现在输出 `readiness_gap_ids` 与 `capture_gate_gap_ids`，让采集者直接看到每个 readiness state 或真实事件门槛下的具体 gap id，不需要从全量 item 表人工筛选。
- 2026-05-25：pending `next_capture_focus`、append review 和 placeholder replacement review 现在都通过 `harness_collection_lane_commands.py` 输出带当前 `ledger_action` 的 lane-specific planner / intake command，避免通用 `--gap-id` scope 跑到错误 lane 或返回空 intake。
- 2026-05-25：collection config 和 sample follow-up coverage 现在要求 active real-sample area / priority 的 planner capture-card、template drift、intake summary、readiness audit 与 `--capture-focus-area` / `--capture-focus-priority` 命令进入 routed/required command package；当前 6 个 area 与 4 个 priority 均闭合，只防命令包漂移，不运行命令或生成样本。

## 升级规则

- 一个 advisory 或 review-required check 不能直接升为 blocking。
- 升级前必须至少有两次真实样本，且记录误报、修复路径、CI 成本和 reviewer 负担。
- `check_burn_in_ledger.py` 校验证据账本结构和升级决策一致性；`Accepted samples` 仍必须来自真实样本，不能由账本检查本身生成。
- 升级决策进入 `status` 或 `ADR`；`AGENTS.md` 只补触发句，不展开完整执行细节。
