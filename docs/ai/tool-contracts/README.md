# Standard Tool Contracts

This directory defines the repo-local standard contract registry for harness tools and future MCP-like tools.

The registry is intentionally small, dependency-free, and readable by hooks, CI, and agents. It describes what a tool is allowed to do before automation decides whether it may run unattended, needs a dry run, or requires a human confirmation.

## Registry Files

- `contracts.json`: canonical machine-readable registry.
- `scripts/check_tool_contracts.py`: stdlib-only validator for local use, hooks, and CI.

## Contract Fields

Each contract in `contracts.json` must include:

- `name`: unique stable identifier. Use lowercase letters, numbers, dots, underscores, or hyphens.
- `purpose`: short human-readable reason the tool exists.
- `path`: repo-relative path to the tool entrypoint.
- `command`: default safe invocation from the repo root.
- `inputs`: list of expected input classes, such as repository files, git state, runtime observations, or GitHub API state.
- `outputs`: list of expected output classes, such as stdout report, JSON report, markdown draft, or process exit code.
- `side_effects`: list of side-effect classes from the enum below.
- `permissions`: list of required permissions or preconditions. Use plain strings such as `read-repo`, `write-runtime`, `github-read`, or `human-confirmation-required`.
- `timeout_seconds`: expected upper bound for normal automation. Must be between 1 and 3600.
- `destructive`: `true` only when the default command can delete, overwrite, close, publish, push, merge, or otherwise destroy state.
- `externally_visible`: `true` when the default command can create externally visible effects outside the local machine.
- `automation_mode`: highest unattended mode allowed for the default command.
- `verification_commands`: commands that validate the contract or the tool's normal safe path.

Optional fields are allowed for extra explanation. Current registry entries use:

- `dangerous_flags`: flags that change the default safe invocation into a destructive or externally visible action.
- `notes`: short caveats for agents and reviewers.

## Side-Effect Classes

Allowed `side_effects` values:

- `none`: no repo, runtime, network, browser, git, or GitHub side effect beyond stdout/stderr and exit code.
- `read_repo`: reads repository files.
- `read_runtime`: reads `.codex/runtime/*`.
- `write_runtime`: writes `.codex/runtime/*` or local runtime drafts.
- `write_governance`: writes canonical `docs/ai/*` or `docs/requirements/*`.
- `write_worktree`: writes non-runtime files in the working tree.
- `network_read`: reads from network or package registries.
- `network_write`: writes to a network service.
- `launch_local_server`: starts a local server or listener.
- `browser_automation`: opens or drives a browser.
- `git_read`: reads local or remote git state.
- `git_write`: changes local or remote git state.
- `github_read`: reads GitHub state through `gh` or API.
- `github_write`: writes GitHub state through `gh` or API.

Contracts should describe the default command, not every possible CLI flag. If a normally safe audit command has dangerous flags, list them in `dangerous_flags` and keep `destructive` / `externally_visible` set to the default invocation.

## Automation Modes

Allowed `automation_mode` values:

- `hook`: safe for lifecycle hooks when its timeout is acceptable.
- `ci`: safe for CI or scheduled verification.
- `dry_run`: safe only when invoked in report-only or no-write mode.
- `assistive`: safe for an agent to run during local development after task relevance is established.
- `manual`: should be run manually or with explicit operator intent.
- `human_confirmed`: requires explicit human confirmation before execution.

Destructive default commands must use `human_confirmed` and include `human-confirmation-required` in `permissions`. Externally visible default commands may not use `hook`, `ci`, or `assistive`; use `manual` or `human_confirmed`.

## Hook And CI Usage

Hooks and CI should validate the registry before relying on it:

```bash
.codex/hooks/run_with_repo_python.sh scripts/check_tool_contracts.py
```

Suggested policy:

1. Load `contracts.json`.
2. Select tools whose `automation_mode` is allowed by the current runner.
3. Reject any default command marked `destructive` unless the run has explicit human confirmation.
4. Reject externally visible default commands in hooks and unattended CI.
5. Enforce the declared `timeout_seconds` in the caller.
6. Run the listed `verification_commands` when a tool contract or tool implementation changes.

The validator does not execute registered tools. It checks that the registry is structurally safe enough for a hook, CI job, or agent to make the next decision.

The registry should also include validators that are added by this standard. That keeps the standard checks themselves visible to future hook, CI, and MCP-like automation decisions.

The P0 linter layer is represented as ordinary contracts too: `ruff_python_linter` for Python harness linting and `git_diff_whitespace_check` for whitespace errors in the changed diff.

The local eval and trace adapter layer is also represented as ordinary contracts:

- `run_agent_eval_dataset` records the safe `--dry-run` invocation for CI and review routing. Executing eval checks without `--dry-run` remains an explicit local action.
- `check_threejs_snake_contract` records the no-server static contract for the WS-01 capability sample. It catches missing app files, DOM id drift, importmap drift, smoke API drift and missing REQ/WS metadata.
- `threejs_snake_smoke` and `threejs_snake_blackbox_smoke` record the WS-01 capability validation browser checks. They may start a local static server and drive a browser, but they do not publish externally visible state.
- `export_agent_trace_local` records the local JSON adapter path. It does not perform network export.
- `export_agent_trace_otlp_pilot` records the OTLP HTTP JSON pilot path. Its default command is no-network; `--send` requires an explicit `--endpoint` and produces `network_exported=true` plus HTTP status evidence.
- `check_harness_sample_gap_evidence` records the generic bounded sample ledger for roadmap gaps that do not yet have a dedicated checker. It validates `evidence_refs` as existing repo-relative paths while allowing path selectors such as markdown anchors, pytest node ids, or JSONL line suffixes, and validates `real-interop-run` remote interop candidates with bounded endpoint/status fields while still treating them as review evidence, not proof of hosted collector, OpenAI, MCP, or A2A interoperability.
- `check_harness_sample_followup_coverage` records the advisory drift audit that ensures sample-gap control-plane docs, scripts, tests, governance workflow sample-summary output, template record helpers, shared review context helpers, and readiness routing helpers still trigger `harness-sample-gap-evidence` with a required command list that exactly covers the routed `HARNESS_SAMPLE_GAP_COMMANDS` package, including collection-config, queue, empty-scope regression, intake JSON output, unfiltered intake/readiness baselines, future/local-inclusive state, pending, active area/active priority planner-template-intake-readiness-pending focus, lane/capture-gate/readiness filter, readiness next-collection routing, local-sample-only template skip, replacement, append, outcome, future-work contract, unit-test, workflow-output test, and coverage-audit commands; the workflow-output test now derives active real-sample area / priority / capture-gate / ledger-action / readiness summary commands and corresponding section / `cat /tmp/...` output checks from the current readiness report so CI summary coverage cannot silently lag a new bucket or lane; CI emits markdown + JSON and appends the audit to the step summary.
- `check_harness_collection_config` records the advisory drift audit that keeps roadmap gap ids, collection priorities, trigger text, target ledgers, future-work contract routing, review commands, active capture-gate / ledger-action / readiness CLI choices, focused capture-gate command coverage, active real-sample ledger-action command coverage, active real-sample readiness command coverage, active real-sample area / priority planner-template-intake-readiness-pending focus command coverage, and pending capture-focus area / priority / lane / gate / readiness choices aligned before the planner emits queue entries; focused command templates live in `scripts/harness_collection_command_templates.py`, while active real-sample choice extraction lives in `scripts/harness_collection_command_coverage.py` to keep the checker within code-shape budget.
- `check_warning_sample_code_alignment` records the advisory drift audit that keeps PreToolUse / Stop loop warning finding codes, exported hook constants, sample checker enums, and Stop recommendation mappings aligned; it does not collect evidence or accept pending samples.
- `plan_harness_sample_collection` records the read-only queue used to target the next real sample captures from current gap evidence, including queue summary counts, target checker commands, pending slot status / refs / review blockers, ledger action routing, per-item `readiness_metric_delta` when accepted-real ledger counts differ from the exact readiness source metric, machine-readable `capture_gate` / `capture_gate_detail` preconditions, per-item `evidence_needed`, lane-specific review command fields from `harness_collection_lane_commands` (`replacement_review_command`, `append_review_command`, `outcome_review_command`, `upgrade_decision_review_command`, `contract_precondition_review_command`), item-routing helpers from `harness_sample_collection_items`, area / priority / actionable / ledger-action / pending-state / capture-gate / readiness filters, CI markdown + JSON queue output rendered through `harness_sample_collection_render`, plus stdout-only pending JSONL templates, upgrade-decision drafts, or future-work contract drafts for focused follow-up. Markdown tables, capture cards, and JSON surface the active lane review command, capture gate, and readiness metric delta so consumers do not infer review routing or readiness satisfaction from `ledger_action` / accepted-real row counts; ready upgrade-decision capture cards show `Next evidence needed` from the decision row `next_evidence_needed` list, and JSON exposes the same list as `evidence_needed`; `--capture-gate` narrows the queue to a specific real-event precondition and `--readiness` narrows it to a readiness state such as `needs-first-real-sample`, `needs-more-real-samples`, `ready-for-upgrade-discussion`, or `local-sample-only` without changing ledgers; governance summary includes focused active area / active priority capture-card views, `--ledger-action append-new-pending-slot`, `--ledger-action fill-existing-placeholder`, remote interop, approved bounded incident, placeholder replacement, security workflow event, bounded real incident, workflow task event, cross-task resume, distinct task-class local trace report, user-confirmed high-impact action, upgrade decision review, needs-first-real-sample, needs-more-real-samples, ready-for-upgrade-discussion, and local-sample-only capture-card views so roadmap buckets, priority buckets, append targets, ADR-017 remote interop capture, ADR-016 approved bounded cascade incident capture, pending placeholder fills, security evidence capture lanes, red-team incident lanes, workflow skill task lanes, cross-task resume capture, distinct local trace task-class capture, user-confirmed high-impact action capture, ready-gap upgrade decision review, first-sample blockers, partial burn-in lanes, accepted-real/readiness metric mismatches, ready-readiness decision drafts, and local-only no-collection boundaries are visible beside the full queue; empty filtered markdown / capture-card scopes print an explicit no-match message and exit successfully so an absent lane is not mistaken for failure or accepted evidence; ready upgrade-decision entries route draft row review to `check_harness_upgrade_decision_candidate.py <candidate-jsonl>` and reuse the existing decision id when one exists; future-work contract-precondition entries also expose `contract_blocker_state` with missing ADR refs, sample collection boundary, next action, and review command, and route draft row review to `check_harness_future_work_contract_candidate.py <candidate-jsonl>`. Approved future-work gaps with `sample_collection_allowed=true` and complete ADR refs leave the contract-precondition lane and route to their dedicated sample target, for example bounded cascade-stop incidents route to the red-team append lane. `fill-existing-placeholder` templates reuse the existing pending sample id so the intended operation is row replacement, not duplicate append; future-work contract templates reuse the existing contract id when one exists for the same reason. Ready-for-upgrade discussion gaps use `review-upgrade-decision` and target `docs/ai/standards/harness-upgrade-decisions.jsonl` instead of append-new-pending-slot. Local-only accepted gaps use `no-sample-collection` so they are visible in inclusive reports without becoming append targets; the local-sample-only capture-card view uses `--include-accepted` to keep that no-collection boundary visible without drafting a sample.
- `check_harness_burn_in_readiness` records the readiness audit for accepted real, cross-task, task-class, and local-only evidence. It now also surfaces active area / priority / gap-id / capture-gate / readiness filters, area / priority / capture-gate counts, `accepted_real_readiness_metric_deltas`, readiness / capture-gate gap-id maps, per-gap `capture_gate` / `capture_gate_detail`, and per-gap next collection routing (`target_artifact`, target checker, `ledger_action`, lane-specific focused planner, lane-specific focused intake, lane review command), so readiness output shows the same roadmap buckets, real-event preconditions, accepted-real/readiness metric mismatches, exact remaining gap ids, and no-write review path as planner, intake, and pending focus without opening those tools first; markdown output includes a `Next Collection Commands` section and JSON carries the same summary maps plus target/checker/planner/intake/lane-review fields. Append, placeholder-fill, upgrade-decision, contract-precondition, and local-only rows therefore expose their no-write review route directly from readiness without writing ledgers, accepting samples, or handing off a generic intake command that returns an empty lane; `--area` and `--priority` can narrow the audit by roadmap bucket, `--gap-id` can narrow the audit to one or more exact gaps, `--capture-gate` can narrow the readiness audit to one or more real-event or review preconditions such as `requires-approved-remote-interop`, `requires-approved-bounded-incident`, `replace-placeholder-after-real-event`, `requires-security-workflow-event`, `requires-bounded-real-incident`, `requires-workflow-task-event`, `requires-cross-task-resume`, `requires-distinct-task-class-report`, `requires-user-confirmed-high-impact-action`, or `upgrade-decision-review`, and `--readiness` can narrow the audit to one or more readiness states such as `needs-first-real-sample`, `needs-more-real-samples`, `ready-for-upgrade-discussion`, or `local-sample-only` without writing ledgers or accepting evidence. Empty filtered scopes report zero matching readiness items instead of implying evidence. Governance CI appends the future/local-inclusive readiness report plus active area, active priority, real capture-gate, upgrade-decision-review, needs-first-real-sample, needs-more-real-samples, ready-for-upgrade-discussion, and local-sample-only sections to the step summary, making hidden roadmap buckets, priority buckets, real-event preconditions, ready-gap decision review, current first-sample blockers, partial burn-in lanes, ready-readiness decision drafts, local-only no-collection boundaries, and their next review commands visible without changing ledgers. It also surfaces ready-gap upgrade decision counts, per-gap decision status, decision refs, missing-decision lists, per-ready-gap `next_evidence_needed`, and top-level `ready_next_evidence_needed_by_gap` from the upgrade-decision ledger so keep-advisory follow-up evidence is visible from readiness output itself; contract-blocked rows reuse future-work `contract_states` to expose missing ADR refs, sample boundary, and next action without approving sampling. Approved future-work contracts with complete ADR refs fall through to ordinary sample readiness such as `needs-first-real-sample`. Strict decision validation remains in `check_harness_upgrade_decisions`.
- `check_harness_sample_templates` records the schema-drift check for generated pending templates, review-ready pending outcome candidates, focused area / priority / ledger-action / pending-state / capture-gate / readiness queues, upgrade-decision drafts, and blocked future-work contract drafts; approved future-work gaps generate ordinary pending sample templates for their dedicated target. Output includes capture gate counts, skipped `no-sample-collection` count / gap ids, and per-validation `capture_gate` / `capture_gate_detail`, so a lane such as `--capture-gate requires-approved-remote-interop`, `--capture-gate requires-approved-bounded-incident`, `--capture-gate replace-placeholder-after-real-event`, `--capture-gate requires-security-workflow-event`, `--capture-gate requires-bounded-real-incident`, `--capture-gate requires-workflow-task-event`, `--capture-gate requires-cross-task-resume`, `--capture-gate requires-distinct-task-class-report`, or `--capture-gate requires-user-confirmed-high-impact-action` can be drift-checked without scanning unrelated templates; `--ledger-action append-new-pending-slot` / `--ledger-action fill-existing-placeholder` expose lane-wide template drift separately, `--ledger-action review-upgrade-decision` can validate ready-gap decision drafts separately, and `--readiness needs-first-real-sample` / `--readiness needs-more-real-samples` / `--readiness ready-for-upgrade-discussion` / `--readiness local-sample-only` can similarly validate first-sample blockers, more-samples lanes, ready decision drafts, and local-only no-collection skip behavior separately. Governance CI appends the default template drift report plus focused active area / active priority reports, append-new-pending-slot, fill-existing-placeholder, remote interop, approved bounded incident, placeholder replacement, security workflow event, bounded real incident, workflow task event, cross-task resume, distinct task-class local trace report, user-confirmed high-impact action, upgrade decision review, needs-first-real-sample, needs-more-real-samples, ready-for-upgrade-discussion, and local-sample-only reports to the step summary. `review-existing-pending-slot` candidates are generated from the original pending ledger row and validated through `check_harness_sample_outcome.py`; local-only `no-sample-collection` rows are skipped and reported instead of converted into placeholder sample drafts. The check does not append, accept, or approve sampling evidence.
- `build_harness_sample_intake_bundle` records the stdout-only grouped draft bundle for actionable gaps without review-ready pending slots. Placeholder pending rows remain eligible for real event capture. It validates each generated template, groups it by target artifact, renders markdown through `harness_sample_intake_render`, annotates entries with target checker commands, `readiness_metric_delta` in text / summary / JSON when accepted-real ledger counts differ from the exact readiness source metric, `capture_gate` / `capture_gate_detail` preconditions, `evidence_needed` capture checklists, pending slot status plus ledger action / refs / review blockers, marks placeholder-fill templates as replacement drafts with the existing pending sample id, routes placeholder fills to `check_harness_placeholder_replacement.py <candidate-jsonl>`, routes append-new-pending-slot entries to `check_harness_sample_append.py <candidate-jsonl>`, treats approved future-work sample lanes the same as ordinary append-new-pending-slot entries, turns review-existing-pending-slot entries into outcome candidates from the original pending row, exposes a dedicated `outcome_review_command` routed to `check_harness_sample_outcome.py <candidate-jsonl>`, exposes a dedicated `upgrade_decision_review_command` for review-upgrade-decision entries routed to `check_harness_upgrade_decision_candidate.py <candidate-jsonl>` before the full `check_harness_upgrade_decisions.py` audit, and shows ready-gap `next_evidence_needed` in Capture Checklist / Upgrade Decision Review summary rows, exposes a dedicated `contract_precondition_review_command` plus `contract_blocker_state` for blocked define-contract-precondition entries routed to `check_harness_future_work_contract_candidate.py <candidate-jsonl>` before the full `check_harness_future_work_contracts.py` audit, supports area / priority / gap / ledger-action / pending-state / capture-gate / readiness filters, prints an explicit no-match message for empty filtered text / summary scopes, and provides a compact `--summary` mode with readiness metric/current-target, readiness metric delta, readiness counts, Capture Gates, a Capture Checklist, and dedicated replacement / append / outcome / upgrade-decision / contract-precondition review sections for CI step summaries without writing ledgers; governance summary includes the default summary plus focused active area / active priority summaries, `--ledger-action append-new-pending-slot --summary`, `--ledger-action fill-existing-placeholder --summary`, `--capture-gate requires-approved-remote-interop`, `--capture-gate requires-approved-bounded-incident`, `--capture-gate replace-placeholder-after-real-event`, `--capture-gate requires-security-workflow-event`, `--capture-gate requires-bounded-real-incident`, `--capture-gate requires-workflow-task-event`, `--capture-gate requires-cross-task-resume`, `--capture-gate requires-distinct-task-class-report`, `--capture-gate requires-user-confirmed-high-impact-action`, `--ledger-action review-upgrade-decision`, `--readiness needs-first-real-sample`, `--readiness needs-more-real-samples`, and `--readiness ready-for-upgrade-discussion` summaries.
- `check_harness_placeholder_replacement` records the no-write review gate for a completed placeholder replacement candidate. The candidate must match an existing pending placeholder id, still belong to the current fill-existing-placeholder lane, remain `pending`, pass its target checker, and become review-ready before an operator replaces the ledger row; the report echoes the current queue's readiness, source metric, current / target, `capture_gate`, `capture_gate_detail`, `evidence_needed`, trigger, boundary, and lane-specific focused planner / intake commands with `--ledger-action fill-existing-placeholder` so reviewers can compare the completed replacement against the exact readiness metric and real-event precondition without hand-deriving the current `--gap-id` route. Review-ready includes shared pending boundary blockers for `no_external_claim`, `local_only`, and `no_network` drift. A passing report points to `check_harness_sample_outcome.py <candidate-jsonl>` for the separate accepted/rejected outcome review, and it does not write or accept evidence.
- `check_harness_sample_append` records the no-write review gate for a completed new pending-row candidate. The candidate must still belong to the current append-new-pending-slot lane, use a non-duplicate sample id, remain `pending`, pass its target checker, and become review-ready before an operator appends the ledger row; the report echoes the current queue's readiness, source metric, current / target, `capture_gate`, `capture_gate_detail`, `evidence_needed`, trigger, boundary, and lane-specific focused planner / intake commands with `--ledger-action append-new-pending-slot` so reviewers can compare the completed row against the exact readiness metric and real-event precondition without hand-deriving the current `--gap-id` route. Review-ready rejects ambiguous shared gap evidence, red-team, local trace boundary fields, and Stage Checkpoint cross-task resume template checkpoint ids before they enter a ledger. A passing report points to `check_harness_sample_outcome.py <candidate-jsonl>` for the separate accepted/rejected outcome review, and it does not write or accept evidence.
- `check_harness_sample_outcome` records the no-write review gate for a completed outcome-change candidate. The candidate must match an existing review-ready pending row, still belong to the current `review-existing-pending-slot` lane, change outcome to `accepted` or `rejected`, keep schema / gap / source type aligned with the pending row, keep sample boundary fields valid, keep stable evidence fields unchanged from the original pending ledger row, and pass its target checker before an operator changes the ledger outcome; the report echoes the current queue's `ledger_action`, readiness, source metric, current / target, `capture_gate`, `capture_gate_detail`, `evidence_needed`, trigger, boundary, and lane-specific focused planner / intake commands so reviewers can confirm the pending row is still in outcome-review scope without hand-deriving the current `--gap-id` route. The focused planner command carries `--ledger-action review-existing-pending-slot`, and the focused intake command also carries `--pending-state with-review-ready-pending` so it does not fall through to the bundle's default without-review-ready-pending scope. It rejects direct placeholder-to-accepted changes, stable evidence field rewrites, and `no_external_claim` / `local_only` / `no_network` boundary drift, and does not write ledgers.
- `check_harness_pending_samples` records pending-slot coverage, separates accepted real samples from synthetic / local-replay / local-only evidence, marks pending rows as review-ready or placeholder, reports queued and actionable ledger action counts / gap lists, reports actionable gaps without review-ready pending, separates actionable sample gaps from future-work contract blockers, ready upgrade-decision gap list, ready upgrade-decision `ready_upgrade_decision_next_evidence_by_gap`, and local-only evidence, emits `contract_blocker_states` only for future-work blockers with missing ADR refs, sample collection boundary, next action, and review command, emits next collection lane commands for placeholder fills, new pending rows, review-ready pending outcome reviews, ready-gap upgrade-decision reviews, and blocked future-work contract preconditions, and emits a bounded `next_capture_focus` list with roadmap area, lane-specific focused planner/intake/review commands carrying each entry's current `ledger_action`, `capture_gate` / `capture_gate_detail` preconditions, readiness state, `readiness_metric_delta` when the ledger accepted-real count differs from the exact source metric, the `evidence_needed` checklist, and placeholder replacement `pending_slot_refs` / `pending_review_blockers` for actionable gaps without review-ready pending. The ready-upgrade next-evidence map comes from the current collection queue / upgrade-decision ledger snapshot, so keep-advisory follow-up evidence is visible beside pending accounting without opening readiness or upgrade-decision audit first. `--capture-focus` renders those entries as compact read-only cards with area, target artifact, target checker, capture gate, readiness, readiness metric delta when present, evidence needed, pending refs, pending blockers, trigger, boundary fields, active area / priority / ledger-action / capture-gate / readiness filters, shown/available focus counts, area / priority / ledger-action / capture-gate / readiness bucket counts, configured limit, truncation status, and hidden gap ids when capped; the default limit is 5, `--capture-focus-limit 0` expands all matching actionable capture lanes, and `--capture-focus-area` / `--capture-focus-priority` / `--capture-focus-ledger-action` / `--capture-focus-gate` / `--capture-focus-readiness` narrow only the focus cards without changing broader pending accounting. Governance CI appends the default focus cards, the full-expansion `--capture-focus-limit 0` cards, active area and active priority capture-focus cards, `--capture-focus-ledger-action append-new-pending-slot --capture-focus-limit 0`, `--capture-focus-ledger-action fill-existing-placeholder --capture-focus-limit 0`, `--capture-focus-gate requires-approved-remote-interop`, `--capture-focus-gate requires-approved-bounded-incident`, `--capture-focus-gate replace-placeholder-after-real-event`, `--capture-focus-gate requires-security-workflow-event`, `--capture-focus-gate requires-bounded-real-incident`, `--capture-focus-gate requires-workflow-task-event`, `--capture-focus-gate requires-cross-task-resume`, `--capture-focus-gate requires-distinct-task-class-report`, `--capture-focus-gate requires-user-confirmed-high-impact-action`, and `--capture-focus-readiness needs-first-real-sample` / `--capture-focus-readiness needs-more-real-samples` views so hidden lower-priority lanes, roadmap areas, priority buckets, append targets, ADR-017 remote interop capture, ADR-016 approved bounded incident capture, placeholder replacement rows, security evidence capture lanes, red-team real incident lanes, workflow skill task lanes, cross-task resume capture, distinct local trace task-class capture, user-confirmed high-impact action capture, first-sample blockers, and partial burn-in lanes are visible in the step summary. JSON output includes `next_capture_focus_area_filter`, `next_capture_focus_priority_filter`, `next_capture_focus_ledger_action_filter`, `next_capture_focus_capture_gate_filter`, `next_capture_focus_readiness_filter`, `next_capture_focus_count`, `next_capture_focus_available_count`, `next_capture_focus_limit`, `next_capture_focus_truncated`, `next_capture_focus_hidden_gap_ids`, and shown/available area / priority / ledger-action / capture-gate / readiness count fields, while each `next_capture_focus` entry includes `area`, `capture_gate`, `capture_gate_detail`, `readiness`, `readiness_metric_delta`, `evidence_needed`, `pending_slot_refs`, and `pending_review_blockers`, so consumers can distinguish capped output from a complete queue, see active filters, and see exact hidden gap ids plus the roadmap area, real-event precondition, readiness state, accepted-real/readiness mismatch if present, bounded evidence fields, and placeholder row refs/blockers without opening the intake bundle or review cards first. It routes append-new-pending-slot lanes to `check_harness_sample_append.py <candidate-jsonl>`, routes review-ready pending lanes/cards to `check_harness_sample_outcome.py <candidate-jsonl>`, routes review-upgrade-decision lanes to `check_harness_upgrade_decision_candidate.py <candidate-jsonl>` and then `check_harness_upgrade_decisions.py`, keeps local-only accepted gaps in `no-sample-collection`, and can emit read-only pending review cards filtered by gap or review state that bind each slot to the relevant checker, evidence class, review state, review blockers, ledger action, capture gate, trigger, evidence-needed checklist, replacement/outcome review command, and readiness state. Approved future-work gaps are counted as actionable sample gaps rather than contract blockers. Pending review blockers include `no_external_claim`, real/local `local_only`, red-team boundary, local trace `no_network` drift, and Stage Checkpoint cross-task resume template checkpoint drift so ambiguous pending rows remain placeholders. Empty filtered text output and card sets print explicit no-match messages.
- `check_harness_future_work_contracts` records the read-only contract precondition gate for future-work gaps and emits CI markdown + JSON reports. JSON includes per-gap `contract_states` with ADR refs, `missing_adr_refs`, sample collection boundary, next action, and review command so future-work blockers are machine-routable. It rejects duplicate `gap_id` rows so contract drafts replace the current row instead of being appended beside it. Contracts stay blocked until ADR / contract approval explicitly allows sampling; `approved-for-sampling` rows must point at an existing adopted repo ADR that mentions the `gap_id`, contract id, `auth_model`, `endpoint_or_authority_scope`, `redaction_or_boundary_model`, and `cost_or_stop_boundary`. Approval only opens the bounded pending-sample path; it does not prove the future-work behavior.
- `check_harness_future_work_contract_candidate` records the no-write review gate for a single blocked future-work contract candidate. It requires the candidate to pass the future-work contract checker, target a current `define-contract-precondition` gap when one exists, reuse the existing contract id before an operator replaces a contract row, and pass the same adopted ADR coverage checks before `approved-for-sampling`; the report re-resolves the current queue lane and echoes `ledger_action`, readiness, source metric, current / target, `capture_gate` / `capture_gate_detail`, `evidence_needed`, trigger, boundary, and lane-specific focused planner / intake commands so approved contracts that now route to `append-new-pending-slot` are visibly stale for contract-candidate review without hand-deriving the current `--gap-id` route or running an empty generic intake scope. Approved rows that have left the contract-precondition lane remain covered by the full contract checker, the current define-contract queue may be empty when all future-work contracts are approved, and passing the candidate gate does not collect or accept samples.
- `check_harness_upgrade_decisions` records the strict advisory audit for ready-gap keep/promote/defer rows. It requires every `ready-for-upgrade-discussion` gap to have a bounded decision row, validates source metric / accepted count / upgrade target against the current readiness snapshot, rejects duplicate or extra gap decisions, requires `evidence_refs` to be existing repo-relative paths, forbids local runtime references, and exposes per-gap `next_evidence_needed` in text / JSON output so keep-advisory follow-up evidence stays machine-readable. It does not write ledgers, create ADRs, collect samples, accept evidence, or upgrade checks.
- `check_harness_upgrade_decision_candidate` records the no-write review gate for a single upgrade-decision candidate. It requires the candidate to pass the upgrade-decision checker, include `next_evidence_needed`, target a current `review-upgrade-decision` gap, match current readiness snapshot counts, and reuse the existing decision id before an operator replaces a decision row; the report also re-resolves the current queue lane and echoes `ledger_action`, readiness, source metric, current / target, `capture_gate` / `capture_gate_detail`, `evidence_needed`, trigger, boundary, and lane-specific focused planner / intake commands with `--ledger-action review-upgrade-decision` so reviewers do not rely on candidate self-description, hand-written `--gap-id` commands, or a generic intake scope that returns no ready-gap decision rows. Passing it does not write ledgers, create ADRs, collect samples, or upgrade checks.
- `check_burn_in_ledger` records CI validation for blocking-candidate ledger coverage and emits CI markdown + JSON reports. The output now includes decision counts, total remaining sample slots, checks still needing samples, upgrade-eligible checks, upgrade-review-needed checks, and per-check rows with accepted sample counts, remaining samples, evidence refs, current decision, next evidence, repair path, cost, false-positive note, upgrade eligibility, and upgrade-review-needed state, so blocking-candidate calibration can be routed without manually parsing the markdown ledger. Accepted samples greater than zero must carry existing repo-relative evidence refs. `2/2` keep-candidate rows must route next evidence to upgrade decision review. Upgrade-eligible only means the sample target is met; it does not upgrade checks or create accepted samples.
- `check_burn_in_upgrade_decisions` validates the bounded decision ledger for blocking-candidate checks that reached `2/2` but remain `keep-candidate`. It reads `docs/ai/standards/check-burn-in-upgrade-decisions.jsonl`, compares rows against `check_burn_in_ledger` `upgrade_review_needed_checks`, rejects stale sample snapshots, non-existing repo-relative evidence refs, or local runtime references, and reports missing / extra decisions in CI markdown / JSON output. It does not change check levels, write ADRs, collect samples, or make checks blocking.
- `collect_harness_sample_gaps` records the sample-gap collector used to target future real-world security, guardrail, workflow, and trace evidence; it also summarizes current bounded sample-checker counts where available and emits CI markdown + JSON reports.
- `check_workspace_sandbox` records the local workspace sandbox / rehydration manifest checker. It validates local boundaries and forbidden external-interop claims; it does not create a native sandbox provider or prove OpenAI, MCP, A2A, or OTLP interoperability.

## Stop Trace Producer Contract

`stop_runtime_observation` is registered as a hook-safe local runtime writer. Its default command may append sanitized Stop observations and local `agent-trace/v1` JSONL under `.codex/runtime/observations/`, so the contract declares `write_runtime` and `write-runtime`.

The contract is deliberately local-only. It does not claim OpenTelemetry, OpenAI, or any remote trace exporter integration; those remain future work that would need a separate contract and validation path.
